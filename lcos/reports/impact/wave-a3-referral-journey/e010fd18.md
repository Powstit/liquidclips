# Commit Impact Report

**Branch:** `wave-a3/referral-journey`
**Commit SHA:** `e010fd18aeb41668f82001c7f452bd126709a316`
**Base commit:** `dc44039d`
**Author (LCOS wave owner):** Train A3 agent (dispatched by integration lead)
**Cluster:** `cluster-2.monetisation-and-whop` · BC-004 (business journey with no canonical owner)
**Bugs targeted:** `[BUG-017]`
**Time:** `2026-07-12T09:23Z`

---

### 1. Files changed

| File | Change type | Owner (LCOS) | Line-range |
|---|---|---|---|
| `desktop-2/src/routes/wallet-detail/WalletDetail.tsx` | edit | `feature.wallet-detail` (Section pipeline · money surface) | +150 / -13 (imports, `referralUrl` derive, `WalletReferralBlock` wrapper, seams, telemetry) |
| `desktop-2/src/routes/wallet-detail/referral.journey.test.ts` | add | `feature.wallet-detail` regression suite | +164 |
| `lcos/04_JOURNEY_BIBLE/j010-referral.md` | add | `capability.affiliate-revenue` · Journey Bible | +325 |
| `lcos/09_BUG_LEDGER.md` | edit | Ledger | +18 / -4 (BUG-017 → FIXED_UNPROVEN + gap docs) |
| `lcos/graph/bugs.json` | edit | Ledger (machine index) | +76 / -18 (targeted BUG-017 mutation) |

### 2. Canonical owner change

**Before:** `state.affiliate-attribution` was PROPOSED but not registered — no owner journey · no owning station · no test seam.
**After:** `state.affiliate-attribution` remains PROPOSED (per BUG-017 chain_links · P4-owed formal registration), but the operational owner is now j010-referral, owner-station `station.wallet.referral-affordance-mounted`, owner-file `desktop-2/src/routes/wallet-detail/WalletDetail.tsx::WalletReferralBlock`.
**Duplicate writers removed:** none (AffiliateWidget already OWNED the copy/QR internals; Train A3 layered j010 telemetry + DOM seams on TOP without duplicating the fetch/click logic).
**Duplicate writers preserved (with justification):** `AffiliateWidget` continues to emit its `affiliate_link_copied` + `referral_qr_downloaded` topics (AU-B-6 Money Funnel path) alongside the new j010 topics. Preserved because the AU-B-6 topics feed a DIFFERENT HQ consumer (Money Funnel) and renaming them would break that path. The j010 topics are the canonical journey names; the AU-B-6 topics are surface-scoped instrumentation.

> **Wave contract rule:** this section does NOT synchronise two writers — the two topic families serve two distinct consumers and were intentionally left as a two-topic emit per click.

### 3. Chain-link updates

| Layer | Before | After | Delta |
|---|---|---|---|
| Mission fingerprint | `[M1, M2]` (declared but unproven) | `[M1, M2]` (proven at the mount+copy+qr stations) | Journey now testable → mission fingerprint has provable per-station attribution |
| Capability | `capability.affiliate-revenue` (unowned) | `capability.affiliate-revenue` (j010 as canonical owner) | Owner named |
| Feature | `feature.wallet-referral-block` (implicit) | `feature.wallet-referral-block` (explicit · owned by WalletReferralBlock) | Feature ID crystallised |
| Journey | j010-referral (unauthored) | j010-referral (authored · AMBER) | +1 authored journey |
| Station | none | +8 stations authored (mount → copy/qr → outbound → external → attribution → ledger → hq) | +8 |
| Canonical state | `state.affiliate-attribution` (proposed) | `state.affiliate-attribution` (proposed · gap flagged) | unchanged formal state; gap now DOCUMENTED (was silent) |

### 4. Journeys affected

| Journey ID | Before status | After status (predicted) | Verification method |
|---|---|---|---|
| `j010-referral` | UNAUTHORED (silent RED) | AMBER (client-side proof + documented backend persistence gap) | referral.journey.test.ts (source-contract · 15 assertions) + manual walk owed |
| `j005-first-payout-eligibility` | AMBER | AMBER (unchanged) | orthogonal · no edits |
| `j012-wallet-view-balance` | AMBER | AMBER (unchanged) | orthogonal · WalletReferralBlock is layered in without touching ledger code |

### 5. Golden paths affected

| Path | Impact |
|---|---|
| referral funnel (unmapped in golden-path registry) | Now has a canonical journey file + seams + tests. Registry entry still P4-owed. Doctor should surface a `gap:golden-path-registry-referral-funnel` follow-up. |
| wallet money surface | Unchanged behavior; new inert DOM wrapper + telemetry emit. No customer-visible copy change. |

### 6. Business capabilities affected

| Capability | Impact | Mission fingerprint delta |
|---|---|---|
| `capability.affiliate-revenue` | Now has a canonical owner journey + test seams + telemetry topics. Attribution surface is Doctor-observable. | M1 (Reach) + M2 (Revenue) now provable per-station rather than declaratively asserted. |

### 7. Telemetry

**Topics added or changed:**
| Topic | Payload | Consumer | Persistence (stdout · HQ) |
|---|---|---|---|
| `referral_affordance_mounted` | `{has_referral_url:bool, has_affiliate_id:bool, ts:number}` | Doctor · HQ Money Funnel tile (P4-owed) | stdout-only (`/telemetry/diagnostic`) |
| `referral_link_copied` | `{referralUrl:string|null, ts:number}` | Doctor · HQ Money Funnel tile | stdout-only |
| `referral_qr_generated` | `{ts:number}` | Doctor · HQ Money Funnel tile | stdout-only |
| `referral_attribution_recorded` | `{affiliate_id, ts}` | HQ money-funnel tile (P4-owed) | **NOT wired backend-side** · client-side best-effort only · `gap:j010-attribution-persistence` |
| `referral_qr_scan_detected` | `{ts}` | HQ (best-effort) | **NOT wired** · client cannot observe in-flight scans · P4-owed |

**Topics deleted:**
| Topic | Reason | Backfill test |
|---|---|---|
| _none_ | pre-existing `affiliate_link_copied` + `referral_qr_downloaded` PRESERVED — different consumer (AU-B-6 Money Funnel) | n/a |

### 8. Regression tests

**Added:**
| Test ID | File | Protects (node.id or edge) |
|---|---|---|
| `mounts-and-fires-affordance-telemetry` (3 assertions) | `desktop-2/src/routes/wallet-detail/referral.journey.test.ts` | `WalletReferralBlock` mount effect · `referral_affordance_mounted` topic |
| `copy-link-fires-telemetry` (3 assertions) | same | Click-capture bridge · `referral_link_copied` topic |
| `qr-render-fires-telemetry` (3 assertions) | same | `qrEmittedRef` guard · `referral_qr_generated` topic |
| `data-referral-link-present-and-copies-referralurl` (5 assertions) | same | Three DOM seams + `referralUrl` derivation |
| `does NOT touch six-state cancellation` (1 assertion) | same | Scope guard for Train C2 territory |

**Modified:**
| Test ID | Reason |
|---|---|
| _none_ | orthogonal to existing suites |

**Deleted:**
| Test ID | Justification |
|---|---|
| _none_ | n/a |

### 9. Risk assessment

- **Risk level:** LOW
- **Blast radius:** WalletDetail render tree only. `AffiliateWidget` is DOM-wrapped without prop changes; six-state cancellation code UNTOUCHED (Train C2 territory); ledger/summary code UNTOUCHED.
- **Shell impact:** none (pure-frontend edits · no Rust · no Cargo · no tauri.conf · no new npm dep).
- **Ship-lens P0/P1 predicted:** none (money-surface rule satisfied; approved mockup + founder video + 3+ states unchanged from base).
- **Rollback complexity:** LOW (`git revert e010fd18 82923778 7bcc0616` in reverse order OR `git reset --hard dc44039d`).

### 10. Rollback plan

**Pre-commit hash:** `dc44039d`
**Rollback command:** `git reset --hard dc44039d` (branch is 3 commits ahead of base · linear · no merges)
**Data-migration reverse:** none (no schema change · documented persistence gap explicitly deferred)
**Telemetry cleanup on rollback:** New topics `referral_affordance_mounted`, `referral_link_copied`, `referral_qr_generated` would stop emitting — HQ consumers should treat their absence as neutral (Doctor's mount-missing flag would raise for j010 but that's the pre-BUG-017 status quo).
**Verification after rollback:** `pnpm test` in `desktop-2/` returns 328 green (drops 15 j010 tests). `grep -c 'referral_affordance_mounted' desktop-2/src/routes/wallet-detail/WalletDetail.tsx` returns 0.

### 11. Bug status transitions (post-merge)

| Bug ID | Before | After | Blocks CLOSED on |
|---|---|---|---|
| `BUG-017` | OPEN | FIXED_UNPROVEN | (1) live-walk observation of `referral_affordance_mounted` + `referral_link_copied` on a real desktop install (currently owed by Doctor Full); (2) HQ persistence migration for `referral_attribution_recorded` (P4-owed · `gap:j010-attribution-persistence`); (3) Doctor Full re-run confirming journey file + seams + tests present on merged branch. |

> **Reminder:** No bug flips to CLOSED in an Impact Report. Only Doctor Full may CLOSED · after scanners.

### 12. Live customer walkthrough steps (for FIXED_UNPROVEN → CLOSED prep)

Ordered steps a human tester runs on the promoted bundle:

1. Install RC1 bundle · open Liquid Clips · sign in with a JWT-holding test account. → Wallet route renders. → `[LC-CLIENT-DIAG]` line with `topic=referral_affordance_mounted` observed in Railway backend logs within ~2s.
2. Claim a handle via the AffiliateWidget rename flow (if not already claimed). → `me.handle` populates. → `[LC-CLIENT-DIAG]` line with `topic=referral_qr_generated` observed.
3. Click the "Copy" button next to the share URL. → "Copied ✓" badge flashes. → Clipboard contains `https://liquidclips.app/join/<handle>`. → `[LC-CLIENT-DIAG]` line with `topic=referral_link_copied data={referralUrl:"https://liquidclips.app/join/<handle>", ts:...}` observed.
4. Verify all three DOM seams via DevTools:
   - `document.querySelector('[data-referral-link][data-copy-value]')` returns the wrapper with the exact copy URL as its value.
   - `document.querySelector('[data-referral-qr]')` returns the QR wrapper.
   - `document.querySelector('[data-referral-attribution-source]')` returns the outer wrapper with `data-source` populated to the user's `affiliateId`.
5. (Optional) Complete a full external conversion (paste URL in another browser · convert on Whop) — confirm the Whop membership webhook fires (this exits j010 to `station.attribution_receiver_backend`). Note: `referral_attribution_recorded` topic emit is deferred per persistence gap.

### 13. Doctor Lite verdict

```
LCOS DOCTOR (LITE) · not run in this Impact Report (Train A3 deliverable does not include Doctor Lite invocation per RC1 dispatch scope · Doctor Full will run on integration merge)
```

### 14. Reviewer sign-off

- **Ship-lens:** not run inside Train A3 worktree (money-surface rule preserved · approved HTML at `desktop-2/docs/mockups/approved/wallet-detail.html` unchanged · founder video unchanged · 3+ states preserved).
- **Human reviewer:** pending integration lead review post-merge.
- **Rollback rehearsed:** no (LOW-risk pure-frontend change).
- **Merged:** no (local only per RC1 dispatch instructions).
- **If merged:** post-merge Doctor Lite should confirm graph freshness, j010-referral node presence, BUG-017 status = FIXED_UNPROVEN.

### 15. Bug class + class-elimination progress (DECISION-0011)

| Bug ID | Bug class | Class-elimination pattern applied | Class instances eliminated by this commit | Class status after commit | Deferred class-elimination ticket |
|---|---|---|---|---|---|
| `BUG-017` | BC-004 (business journey with no canonical owner) | Author journey file with station chain + stable DOM seams + telemetry topics + regression suite | 1 (j010-referral) | in-progress · sibling BUG-004 + BUG-014 remain open under BC-004 | n/a (this commit IS the class-elimination for j010) |

### 16. Eight-question auto-answers per DECISION-0010

Per bug transitioned:

1. **Golden paths blocked (before → after):** `referral funnel (unmapped)` → still unmapped in registry but now has a canonical journey + seams + tests; registry entry P4-owed as `gap:golden-path-registry-referral-funnel`.
2. **Business capabilities degraded (before → after):** `capability.affiliate-revenue` was silently vulnerable to UI-reshuffle drift → now Doctor-observable via 3 telemetry topics + 3 DOM seams + 15 regression assertions.
3. **Canonical states affected:** `state.affiliate-attribution` (proposed) — unchanged formal registry entry; operational owner now named (j010-referral).
4. **Journeys that fail (before → after):** j010-referral was UNAUTHORED (silent RED) → now AMBER (client-side proof + backend persistence gap documented).
5. **Telemetry that should have detected:** `referral_link_copied`, `referral_qr_scanned`, `referral_attribution_recorded` (all three now emit, with the third best-effort client-side; backend authoritative emit is P4-owed).
6. **Tests that should have failed (before → after):** none existed (`tests_should_have_failed: []` in original bugs.json) → 15 new assertions in `referral.journey.test.ts` now guard the seams + telemetry contract.
7. **Sibling bugs by root cause:** BUG-004, BUG-014 (BC-004 umbrella · unowned monetisation journeys). Both remain OPEN — closing them is out of Train A3 scope.
8. **Permanent architectural fix (referenced in §15):** BC-004 class-elimination pattern applied · journey file + station chain + telemetry topics + stable DOM seams + source-contract regression suite.

### 17. Provenance record

| File | Origin | Reviewed? | Provenance verdict |
|---|---|---|---|
| `desktop-2/src/routes/wallet-detail/WalletDetail.tsx` | Train A3 (2026-07-12 · this session) — targeted edit inside referral block only (line ~881) + new `WalletReferralBlock` component at end of file. No edits to six-state cancellation, wallet summary, or ledger code. | self-reviewed against BUG-017 close conditions + Train A3 scope rules | permanent · isolated · scoped to referral block per Train A3 stop conditions |
| `desktop-2/src/routes/wallet-detail/referral.journey.test.ts` | Train A3 (new file · this session) — source-contract regression suite mirroring the `CrewOnboarding.test.ts` + `useMe.test.ts` grep pattern already shipped in the codebase. | self-reviewed against journey file's declared acceptance test IDs | permanent · isolated |
| `lcos/04_JOURNEY_BIBLE/j010-referral.md` | Train A3 (new file · this session) — authored per DECISION-0010 station chain against the Journey Bible schema at `04_JOURNEY_BIBLE/README.md`. Slot `j010-*` in the README planning list was `submit-to-whop`; Train A3 dispatch explicitly named `j010-referral`, honoring the dispatch directive over the plan. | self-reviewed against BUG-017 `closes_only_when` conditions | permanent · will need reconciliation with the README's slot-name (submit-to-whop → j0??-submit-to-whop) in a follow-up · noted as `gap:journey-bible-readme-slot-rename` |
| `lcos/09_BUG_LEDGER.md` | Train A3 (edit · this session) — BUG-017 block: status OPEN → FIXED_UNPROVEN + `fixed_unproven_notes` populated + class-elimination annotations + documented gaps. | self-reviewed against DECISION-0011 wave contract | permanent · isolated |
| `lcos/graph/bugs.json` | Train A3 (edit · this session) — surgical string-slice update of the BUG-017 object only. Two prior commits fixed encoding + diff-scope issues before landing this final form. | self-reviewed against schema 1.2.0 | permanent · isolated (only BUG-017 fields mutated) |

---

## Deferred class-elimination follow-ups (out of Train A3 scope)

- `gap:j010-attribution-persistence` — backend `referral_attribution_recorded` topic authored but persistence table not written · P4-owed schema migration.
- `gap:j010-hq-tile` — HQ Money Funnel tile does not yet surface j010 exit event · blocked on above.
- `gap:golden-path-registry-referral-funnel` — registry entry for referral funnel not yet created.
- `gap:journey-bible-readme-slot-rename` — README `j010-*` slot was `submit-to-whop`; Train A3 dispatch used `j010-referral`. Follow-up rename to avoid future confusion.
