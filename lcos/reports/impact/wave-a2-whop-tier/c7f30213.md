# Commit Impact Report

**Branch:** `wave-a2/whop-tier`
**Commit SHA:** `c7f302136c5d98c9221d751c52474cc77e9237c4`
**Author (LCOS wave owner):** `agent · Train A2 (Claude)`
**Cluster:** `cluster-2.whop-cta-and-tier-propagation`
**Bugs targeted:** `[BUG-004, BUG-014, BUG-008]`
**Time:** `2026-07-12T09:22Z`
**Base commit:** `dc44039d` (integration/cold-entry-mode-b · Phase-0 pack)

---

### 1. Files changed

| File | Change type | Owner (LCOS) | Line-range |
|---|---|---|---|
| `desktop-2/src/design-os/components/WhopStatusChip.tsx` | add | A2 | 1-269 (new) |
| `desktop-2/src/design-os/components/WhopStatusChip.test.ts` | add | A2 | 1-131 (new) |
| `desktop-2/src/design-os/components/TopHud.tsx` | edit | A2 (mount only · A1 owns identity ladder) | +7 (import) · +8 (mount) |
| `desktop-2/src/design-os/components/TopHud.whop-chip.test.ts` | add | A2 | 1-89 (new) |
| `desktop-2/src/design-os/routes/CommandRoom.tsx` | edit | A2 | +1 (import) · +12 (mount block) |
| `desktop-2/src/design-os/routes/CommandRoom.home-whop-cta.test.ts` | add | A2 | 1-87 (new) |
| `desktop-2/src/design-os/state/useTierCaps.ts` | edit | A2 | +31 (StudioTier type + tierToStudioTier + useCanonicalStudioTier) |
| `desktop-2/src/design-os/studio/ExportPanel.tsx` | edit | A2 | -1 prop + hook read (userTier prop deleted, replaced with useCanonicalStudioTier()) |
| `desktop-2/src/design-os/studio/OverlayTemplateGallery.tsx` | edit | A2 | same pattern |
| `desktop-2/src/design-os/studio/ReactionControls.tsx` | edit | A2 | same pattern |
| `desktop-2/src/design-os/export/ExportPanel.tier-propagation.test.ts` | add | A2 | 1-185 (new) |
| `lcos/09_BUG_LEDGER.md` | edit | integration lead schema · A2 status transitions only | BUG-004, BUG-008, BUG-014 status blocks |
| `lcos/graph/bugs.json` | edit | same | BUG-004, BUG-008, BUG-014 status blocks + totals |

### 2. Canonical owner change

**Before:**
- `state.whop-connection` · owner `hook.useMe.snapshot.whopUserId` · writers `[TopHud identityCopy · Settings CTA · Wallet CTA · AffiliateWidget CTA]` (four surface-specific derivations of "should the CTA render?")
- `state.tier` (studio slice) · owner `hook.useTierCaps` · writers `[ExportPanel userTier prop default · OverlayTemplateGallery userTier prop default · ReactionControls userTier prop default · useTierCaps]` (four writers, three of which silently default to `"free"` when no prop passed)

**After:**
- `state.whop-connection` · owner `hook.useMe.snapshot.whopUserId` · writers `[WhopStatusChip · TopHud identityCopy]` (identity pill retained by Wave 1 · new chip is the single persistent-chrome writer for Whop status)
- `state.tier` (studio slice) · owner `hook.useTierCaps` (via `useCanonicalStudioTier()`) · writers `[useTierCaps]` (single canonical writer; studio components are readers only)

**Duplicate writers removed:**
- Three `userTier?: Tier = "free"` prop defaults (ExportPanel · OverlayTemplateGallery · ReactionControls)
- Two conditional-CTA writers for Whop (Settings + Wallet still render CTAs but they now sit BELOW the persistent chip in the chain of visibility · no longer competing for "which surface owns the Whop status?" question)

**Duplicate writers preserved (with justification):**
- TopHud's `identityCopy` retains a "Connect Whop" fork for the pre-Wave-1 identity pill. Wave 1 (A1) owns the pill; A2 was locked out of the identity ladder. The persistent chip mounted alongside the pill is the canonical chrome affordance; the pill's fork is a legacy path Wave 1 will consolidate.
- Settings + Wallet + AffiliateWidget CTAs preserved · they are downstream money surfaces, not chrome affordances. Removing them is out of scope.

### 3. Chain-link updates

| Layer | Before | After | Delta |
|---|---|---|---|
| Mission fingerprint | `[M2]` | `[M2]` | none |
| Capability | `capability.affiliate-revenue`, `capability.content-production` | same | none |
| Feature | `feature.whop-connection`, `feature.export` | same | none |
| Journey | `j004-connect-whop`, `j009-export-single-clip` | same | none |
| Station | `station.tophud.identity-pill`, `station.home.hero`, `station.export.preset-picker`, `station.overlay.template-gallery`, `station.reaction.controls` | + `station.tophud.whop-status` (populated) | +1 station-populated |
| Canonical state | `state.whop-connection`, `state.tier` | same | writers reduced (see §2) |

### 4. Journeys affected

| Journey ID | Before status | After status (predicted) | Verification method |
|---|---|---|---|
| `j004-connect-whop` | RED (CTA absent from persistent chrome) | AMBER (chip mounted · click flow unproven on live device) | Manual walk on unlinked user; Doctor telemetry sample |
| `j009-export-single-clip` | AMBER (tier defaulted to free for Pro/Agency) | AMBER (hook-read canonical tier; live walk on Agency user still owed) | Manual walk with Agency-tier account |

### 5. Golden paths affected

| Path | Impact |
|---|---|
| `gp:whop-linking` | Chip provides persistent affordance from any-state; funnel-entry visibility restored |
| `gp:agency-export` | Agency-tier users' Export panel now renders Agency-unlocked presets/overlays without a caller having to pass a prop |

### 6. Business capabilities affected

| Capability | Impact | Mission fingerprint delta |
|---|---|---|
| `capability.affiliate-revenue` | Positive · MRR gate visible from every JWT-holding state | M2 unchanged |
| `capability.content-production` | Positive · Pro/Growth/Agency users get tier-correct export gating without caller cooperation | M2 unchanged |

### 7. Telemetry

**Topics added or changed:**
| Topic | Payload | Consumer | Persistence (stdout · HQ) |
|---|---|---|---|
| `whop_status_chip_impression` | `{ state, mount_site }` | Doctor / HQ | stdout · HQ (via `/telemetry/diagnostic` in lcDiag) |
| `whop_connect_cta_clicked` | `{ mount_site }` | Doctor / HQ | same |
| `whop_status_transition` | `{ from, to, elapsedMs, mount_site }` | Doctor / HQ | same |

**Topics deleted:**
| Topic | Reason | Backfill test |
|---|---|---|
| — | none deleted | n/a |

### 8. Regression tests

**Added:**
| Test ID | File | Protects (node.id or edge) |
|---|---|---|
| `WhopStatusChip.test.ts::four-state-derivation-contract` (4 assertions) | `desktop-2/src/design-os/components/WhopStatusChip.test.ts` | `state.whop-connection` derivation |
| `WhopStatusChip.test.ts::component-wiring-contract` (7 assertions) | same | chip → useMe/useAuth/connectWhop wire |
| `TopHud.whop-chip.test.ts` (7 assertions) | `desktop-2/src/design-os/components/TopHud.whop-chip.test.ts` | chip mount + identity ladder untouched |
| `CommandRoom.home-whop-cta.test.ts` (6 assertions) | `desktop-2/src/design-os/routes/CommandRoom.home-whop-cta.test.ts` | Home hero mount + unlinked-only render |
| `ExportPanel.tier-propagation.test.ts` (14 assertions) | `desktop-2/src/design-os/export/ExportPanel.tier-propagation.test.ts` | 3 studio components read canonical tier + no caller passes prop |

**Modified:**
| Test ID | Reason |
|---|---|
| — | none modified (existing TopHud tests unchanged; TopHud.pill.test.ts already asserts no `userTier=` in ExportRoute/AppShell — still green) |

**Deleted:**
| Test ID | Justification |
|---|---|
| — | none deleted |

### 9. Risk assessment

- **Risk level:** LOW
- **Blast radius:** Two chrome surfaces (TopHud + Home hero) + three studio components. All changes are additive (new hook / new component / prop deletion). No shell code, no Rust, no backend.
- **Shell impact:** none
- **Ship-lens P0/P1 predicted:** none — every change replaces or eliminates a class-of-drift writer per BC-002.
- **Rollback complexity:** LOW · single-commit revert restores prior behaviour cleanly.

### 10. Rollback plan

**Pre-commit hash:** `dc44039d`
**Rollback command:** `git revert c7f30213`
**Data-migration reverse:** none (no schema or state persistence changes)
**Telemetry cleanup on rollback:** remove `whop_status_chip_impression`, `whop_connect_cta_clicked`, `whop_status_transition` from HQ consumer dashboards (none built yet — no cleanup required)
**Verification after rollback:** `vitest run` + `tsc --noEmit` green; TopHud + Home hero return to pre-chip state; studio components re-accept userTier prop.

### 11. Bug status transitions (post-merge)

| Bug ID | Before | After | Blocks CLOSED on |
|---|---|---|---|
| `BUG-004` | OPEN | FIXED_UNPROVEN | Doctor observes `whop_connect_cta_clicked { mount_site: "top-hud" }` from live install; Daniel confirms chip visible in TopHud strip |
| `BUG-014` | OPEN | FIXED_UNPROVEN | Doctor observes `whop_status_chip_impression { mount_site: "home-hero" }` on unlinked account; Daniel confirms hero CTA visible on Home |
| `BUG-008` | OPEN | FIXED_UNPROVEN | Live walk on Agency-tier account shows Agency-unlocked ribbons in Export + Overlay + Reaction (no `userTier` prop passed anywhere) |

> **Reminder:** No bug flips to CLOSED in this Impact Report.

### 12. Live customer walkthrough steps (for FIXED_UNPROVEN → CLOSED prep)

1. Install ship-side desktop bundle → open app → sign in with a fresh (JWT-only) account → expected UI: TopHud pill strip shows fuchsia "Connect Whop" chip · expected telemetry: `whop_status_chip_impression { state: "unlinked", mount_site: "top-hud" }`
2. Same account → navigate to Home → expected UI: full "Connect Whop to activate paid clips" hero card between banner and tile grid · expected telemetry: `whop_status_chip_impression { state: "unlinked", mount_site: "home-hero" }`
3. Click TopHud chip → expected: OAuth window opens (via `connectWhop()`) · expected telemetry: `whop_connect_cta_clicked { mount_site: "top-hud" }`
4. Complete Whop OAuth → deep-link back → expected: chip flips to green "Whop linked", Home hero card disappears · expected telemetry: `whop_status_transition { from: "unlinked", to: "linked", elapsedMs, mount_site: "top-hud" }`
5. Sign in with Agency-tier account (real-http source) → open Workstation → Export panel → expected UI: agency-unlocked ribbons on presets, no "Locked" state on custom preset · expected telemetry: none (behavioral)
6. Same account → Overlay picker + Reaction controls → expected UI: agency-unlocked layouts, no "Locked" glyphs

### 13. Doctor Lite verdict

```
LCOS DOCTOR (LITE) · not run this session · integration lead to run post-merge per matrix step 4
```

### 14. Reviewer sign-off

- **Ship-lens:** deferred to integration lead post-merge per matrix
- **Human reviewer:** integration lead (Claude) · pending
- **Rollback rehearsed:** no (single-file revert path documented)
- **Merged:** no (Local A2 branch only; matrix step 2 = A1 → A2 → A3 sequential merge into `integration/cold-entry-mode-b`)
- **If merged:** post-merge Doctor Lite must confirm graph freshness restored + no new invariant violations

### 15. Bug class + class-elimination progress (DECISION-0011)

| Bug ID | Bug class | Class-elimination pattern applied | Class instances eliminated | Class status after commit | Deferred class-elimination ticket |
|---|---|---|---|---|---|
| `BUG-004` | BC-002 (multi-writer state drift · `state.whop-connection`) | Single canonical component (`WhopStatusChip`) reads canonical source (`useMe().snapshot.whopUserId`) and renders every visible Whop CTA. TopHud + Home hero mount the same component. | 1 (persistent chrome affordance) | open (Settings + Wallet + AffiliateWidget still write; scoped out of A2) | none — Wave 3 sweep of downstream money surfaces |
| `BUG-014` | BC-002 | Same pattern · Home hero renders through the same component, gated on `mountSite === "home-hero"` + unlinked-only render. | 1 (home hero affordance) | open (same as above) | same |
| `BUG-008` | BC-002 (multi-writer state drift · `state.tier` studio slice) | `useCanonicalStudioTier()` hook wraps `useTierCaps()` and returns the studio Tier vocabulary. All three studio components dropped the prop with the `"free"` default and read the hook. | 3 (ExportPanel · OverlayTemplateGallery · ReactionControls) | closed-app-wide for the studio slice (no other component in the codebase declares a `userTier?: Tier` prop with a default — see grep in tests) | none |

Total BC-002 class instances eliminated by this commit: **3** (matches sprint contract).

### 16. Eight-question auto-answers per DECISION-0010

#### BUG-004 · Connect Whop not visible from all states

1. **Golden paths blocked (before → after):** `gp:whop-linking` → chrome affordance restored; still owed live walk to prove funnel closes
2. **Business capabilities degraded (before → after):** `capability.affiliate-revenue` MEDIUM → LOW impact (persistent CTA restored; downstream surfaces still support the linking flow)
3. **Canonical states affected:** `state.whop-connection` — owner unchanged (useMe); writer count reduced from 4 to 2
4. **Journeys that fail (before → after):** `j004-connect-whop` fails at station.tophud.identity-pill (overloaded pill hid the CTA for admin/agency/pre-JWT) → chip is orthogonal to identity pill, station.tophud.whop-status now populated
5. **Telemetry that should have detected:** `whop_status_chip_impression`, `whop_connect_cta_clicked` — both added in this commit (gap-flag pre-fix: no HQ topic existed)
6. **Tests that should have failed (before → after):** none pre-fix (gap:test) → 4 assertions in WhopStatusChip.test.ts + 7 in TopHud.whop-chip.test.ts + 6 in CommandRoom.home-whop-cta.test.ts
7. **Sibling bugs by root cause:** `BUG-014` (Home hero same class · same fix pattern)
8. **Permanent architectural fix:** single canonical `WhopStatusChip` component reads canonical `useMe().snapshot.whopUserId` + `useAuth().hasJwt`; every persistent-chrome affordance for Whop status routes through it. See §15.

#### BUG-014 · Home hero lacks Whop CTA when unconnected

1. **Golden paths blocked (before → after):** `gp:whop-linking` (home surface entry) → hero CTA present on unlinked; live walk owed
2. **Business capabilities degraded (before → after):** `capability.affiliate-revenue` HIGH → LOW impact (highest-traffic surface now has the CTA)
3. **Canonical states affected:** `state.whop-connection` — same as BUG-004
4. **Journeys that fail (before → after):** `j004-connect-whop` fails at station.home.hero (no CTA rendered) → chip mounted, station populated
5. **Telemetry that should have detected:** `whop_cta_home_impressions` → replaced by `whop_status_chip_impression { mount_site: "home-hero" }` (canonical topic name)
6. **Tests that should have failed (before → after):** none pre-fix (gap:test) → 6 assertions in CommandRoom.home-whop-cta.test.ts
7. **Sibling bugs by root cause:** `BUG-004`
8. **Permanent architectural fix:** shared component with mount-site prop; same fix pattern as BUG-004

#### BUG-008 · ExportPanel + OverlayTemplateGallery + ReactionControls default `userTier="free"`

1. **Golden paths blocked (before → after):** `gp:agency-export` — Pro/Agency users saw Free ribbons if a caller forgot the prop → hook read enforces truth
2. **Business capabilities degraded (before → after):** `capability.content-production` MEDIUM → NONE (source of truth is now canonical)
3. **Canonical states affected:** `state.tier` — owner unchanged; writer count reduced from 4 to 1
4. **Journeys that fail (before → after):** `j009-export-single-clip` degraded at station.export.preset-picker + station.overlay.template-gallery + station.reaction.controls → hook read enforces tier-correct render
5. **Telemetry that should have detected:** none — tier is a caps/gating concern, not an event stream. Behavioral test covers regression.
6. **Tests that should have failed (before → after):** none pre-fix (gap:test) → 14 assertions in ExportPanel.tier-propagation.test.ts
7. **Sibling bugs by root cause:** none in current ledger (BC-002 sweep for studio slice complete)
8. **Permanent architectural fix:** `useCanonicalStudioTier()` in useTierCaps.ts — single canonical hook read replaces the tier-as-prop pattern.

### 17. Provenance record

| File | Origin (commit / session / author) | Reviewed? | Provenance verdict |
|---|---|---|---|
| `WhopStatusChip.tsx` | this commit · agent Train A2 | yes | permanent |
| `WhopStatusChip.test.ts` | this commit · agent Train A2 | yes | permanent |
| `TopHud.tsx` (mount + import) | this commit · agent Train A2 | yes | permanent (respects A1 identity ladder ownership) |
| `TopHud.whop-chip.test.ts` | this commit · agent Train A2 | yes | permanent |
| `CommandRoom.tsx` (mount + import) | this commit · agent Train A2 | yes | permanent |
| `CommandRoom.home-whop-cta.test.ts` | this commit · agent Train A2 | yes | permanent |
| `useTierCaps.ts` (StudioTier + tierToStudioTier + useCanonicalStudioTier additions) | this commit · agent Train A2 | yes | permanent |
| `ExportPanel.tsx` (prop deletion + hook read) | this commit · agent Train A2 | yes | permanent |
| `OverlayTemplateGallery.tsx` (same) | this commit · agent Train A2 | yes | permanent |
| `ReactionControls.tsx` (same) | this commit · agent Train A2 | yes | permanent |
| `ExportPanel.tier-propagation.test.ts` | this commit · agent Train A2 | yes | permanent |
| `lcos/09_BUG_LEDGER.md` (status transitions) | this commit · agent Train A2 | yes | permanent |
| `lcos/graph/bugs.json` (status transitions + totals) | this commit · agent Train A2 | yes | permanent |

Unknown provenance: none.

---

## Test evidence

* `desktop-2` vitest: 40 files · **366/366 tests green** (all 38 new A2 assertions + 328 pre-existing)
* `desktop-2` tsc --noEmit: **0 errors**
* `junior-backend` pytest: 386 passed · 5 pre-existing OTP-flow failures unrelated to A2 (no backend files touched)

## STOP report

**No STOP conditions triggered.**

* Ownership matrix locates `CommandRoom.tsx` under `rooms/` but the file lives at `desktop-2/src/design-os/routes/CommandRoom.tsx`. Same for `overlays/` (actual `studio/`) and `reactions/` (actual `studio/`). This is a documentation drift in the matrix, not a scope conflict — the OWNED files listed in the matrix map 1:1 to actual repo paths. Tests placed alongside actual files so the vitest include (`src/**/*.test.ts`) picks them up.
* Identity ladder untouched (verified by TopHud.whop-chip.test.ts).
* No new npm deps.
* No shell / Rust / backend touches.
* No FORBIDDEN files edited.
