# Commit Impact Report

**Branch:** `wave-b1/runtime-truth`
**Commit SHA:** `a98813ed`
**Base commit:** `d501184b` (Barrier 1 tip on `integration/cold-entry-mode-b`)
**Author (LCOS wave owner):** Agent B1 (Train B · RC1 dispatch)
**Cluster:** `cluster-3.runtime-and-updates`
**Class-elimination target:** BC-002 (single source of truth for "which bundle is rendering") · BC-005 (backend log observability)
**Bugs targeted:** `[BUG-006, BUG-007, BUG-009, BUG-012]`
**Time:** `2026-07-12T10:03Z`

---

### 1. Files changed

| File | Change type | Owner (LCOS) | Line-range |
|---|---|---|---|
| `desktop-2/src/lib/useRuntimeVersion.ts` | edit | `hook.useRuntimeVersion` | 1-166 (rewrite) |
| `desktop-2/src/lib/useRuntimeVersion.test.ts` | add | `hook.useRuntimeVersion` | 1-164 |
| `desktop-2/src/App.tsx` | edit | `shell.App` | 3, 166-171, 199-200, 601-606 |
| `desktop-2/src/lib/telemetry/bootstrap.ts` | edit | `telemetry.bootstrap` | 12-18, 34 |
| `desktop-2/src/sections/diagnostics/DiagnosticsSection.tsx` | edit | `station.diagnostics.version` | 13-19, 28-33, 60 |
| `desktop-2/src/overlays/IntroSplash.tsx` | edit | `station.introsplash.version` | 21-27, 89-92, 456 |
| `desktop-2/src/design-os/routes/Settings.tsx` | edit | `station.settings.version` | 82-88, 137-141, 168-173, 544 |
| `desktop-2/src/components/UpdateBeacon.tsx` | edit | `station.update-beacon.check` | 46-63, 74-149 |
| `desktop-2/src/components/UpdateBeacon.test.ts` | add | `station.update-beacon.check` | 1-99 |
| `junior-backend/app/routes/runtime.py` | edit | `endpoint.get_runtime_manifest` | 63-105 |
| `junior-backend/tests/test_runtime_manifest_shapes.py` | add | `endpoint.get_runtime_manifest` | 1-146 |
| `lcos/09_BUG_LEDGER.md` | edit | ledger | BUG-006 / BUG-007 / BUG-009 / BUG-012 rows |
| `lcos/graph/bugs.json` | edit | ledger index | totals + 4 bug status blocks |
| `lcos/reports/rc1-sprint/STOP_REPORT_WAVE_B1_BUG_012.md` | add | escalation | 1-138 |

### 2. Canonical owner change

**Before:**
- `state.runtime-version` written by 6 disjoint consumers reading `__APP_VERSION__` directly (TopHud pill · Settings copy-diagnostics · DiagnosticsSection · IntroSplash splash · App.tsx boot flowTrace · logDiag · telemetry/bootstrap `release`).
- `state.update-beacon.check` emitted `update_beacon_check_failed` once per failed poll (unbounded).

**After:**
- `state.runtime-version` has ONE owner: `hook.useRuntimeVersion` (React consumers) + `runtimeVersionSync()` (non-React callers). Every render site + every telemetry field reads through the hook or the sync helper.
- `state.update-beacon.check` emits `update_beacon_check_failed` deduplicated by `(step, reason)` fingerprint. First failure of a sad streak lands; identical follow-ups suppressed; healthy read clears the ring.

**Duplicate writers removed:**
- `App.tsx:165` (flowTrace metadata) `__APP_VERSION__` → `runtimeVersionSync()`.
- `App.tsx:198` (logDiag app.boot) `__APP_VERSION__` → `runtimeVersionSync()`.
- `App.tsx:595` (`declare const __APP_VERSION__`) → deleted.
- `telemetry/bootstrap.ts:13, 34` `__APP_VERSION__` → `runtimeVersionSync()`.
- `sections/diagnostics/DiagnosticsSection.tsx:20-24` `VERSION_PLACEHOLDER = __APP_VERSION__` → `useRuntimeVersion().version`.
- `overlays/IntroSplash.tsx:25, 456` `__APP_VERSION__` → `useRuntimeVersion().version`.
- `design-os/routes/Settings.tsx:133, 539-541` `__APP_VERSION__` → `useRuntimeVersion().version`.

**Duplicate writers preserved (with justification):** none.

Wave contract check: this section shows removal, not synchronisation. Compliant.

### 3. Chain-link updates

| Layer | Before | After | Delta |
|---|---|---|---|
| Mission fingerprint | `[M3]` | `[M3]` | 0 |
| Capability | `capability.operational-excellence` | same | 0 |
| Feature | `feature.runtime-updates` · `feature.diagnostics` | same | 0 |
| Journey | `j014-runtime-update` | same | 0 |
| Station | `station.tophud.version-pill` (pre-P1-C) | + `station.settings.version` · `station.introsplash.version` · `station.diagnostics.version` · `station.update-beacon.check` all now read `hook.useRuntimeVersion` | +3 stations onto canonical hook |
| Canonical state | `state.runtime-version` (owner `hook.useRuntimeVersion`) | same, but now with a single writer instead of 6 divergent readers | -5 divergent readers |

### 4. Journeys affected

| Journey ID | Before status | After status (predicted) | Verification method |
|---|---|---|---|
| `j014-runtime-update` | AMBER (3 surfaces drift on hot-swap) | GREEN (single reader, event subscription) | Doctor-lite promote walk on staged bundle |

### 5. Golden paths affected

| Path | Impact |
|---|---|
| Ops · "which version am I on?" | GREEN — Settings copy-diagnostics + DiagnosticsSection stat + IntroSplash tag + TopHud pill all read the same source; support tickets return the truth. |
| Ops · "did the runtime bundle promote?" | Improved — pill / splash / diag stat flip on `lc:runtime-staged` mid-session (via hook subscription). BUG-012 native fix still required to make the URL scheme cache actually serve the new bundle after Cmd+R. |

### 6. Business capabilities affected

| Capability | Impact | Mission fingerprint delta |
|---|---|---|
| `capability.operational-excellence` | version reporting now honest; diag ring no longer flooded by beacon 500s; support triage bandwidth reclaimed | 0 |

### 7. Telemetry

**Topics added or changed:**
| Topic | Payload | Consumer | Persistence (stdout · HQ) |
|---|---|---|---|
| `update_beacon_check_failed` | `{ reason, step }` | HQ + backend log | stdout (rate now bounded — 1 per unique (step, reason) per sad streak) |
| `runtime_version_read_failed` | `{ reason }` | HQ + backend log | stdout (unchanged; hook fallback path) |

**Topics deleted:** none.

### 8. Regression tests

**Added:**
| Test ID | File | Protects (node.id or edge) |
|---|---|---|
| `useRuntimeVersion · non-Tauri fallback` | `desktop-2/src/lib/useRuntimeVersion.test.ts` | `hook.useRuntimeVersion` |
| `useRuntimeVersion · source contract` (5 tests) | same | `hook.useRuntimeVersion` |
| `runtimeVersionSync · non-React callers` (2 tests) | same | `hook.useRuntimeVersion` |
| `BUG-007 grep guard · flagged render sites drop __APP_VERSION__` (6 tests) | same | BC-002 elimination |
| `UpdateBeacon · source contract · Wave B1 BUG-009` (5 tests) | `desktop-2/src/components/UpdateBeacon.test.ts` | `station.update-beacon.check` |
| `UpdateBeacon · documented response shapes` (3 tests) | same | `endpoint.get_runtime_manifest` contract |
| `test_manifest_returns_204_when_no_pass_row` | `junior-backend/tests/test_runtime_manifest_shapes.py` | `endpoint.get_runtime_manifest` |
| `test_manifest_returns_204_when_current_version_matches` | same | same |
| `test_manifest_returns_204_for_unknown_channel` | same | same |
| `test_manifest_returns_204_when_table_missing` | same | BUG-009 core case |

**Modified:** none.
**Deleted:** none.

### 9. Risk assessment

- **Risk level:** LOW
- **Blast radius:** confined to 5 render sites + 1 hook + 1 backend route + 1 beacon polling component. No route boundaries changed, no state shape mutated.
- **Shell impact:** **none** — no Rust / Cargo / tauri.conf / package.json edits. BUG-012 native fix explicitly deferred via STOP report.
- **Ship-lens P0/P1 predicted:** none. Behaviour identical for the healthy path (staged bundle version was already surfaced by the P1-C wire); only the sad path (missing manifest table, mid-session promotion) changes.
- **Rollback complexity:** LOW · single commit revert.

### 10. Rollback plan

**Pre-commit hash:** `d501184b`
**Rollback command:** `git revert a98813ed` OR `git reset --hard d501184b`
**Data-migration reverse:** none (no schema changes).
**Telemetry cleanup on rollback:** none required — `update_beacon_check_failed` shape unchanged (still `{reason, step}`); the change is client-side dedup only.
**Verification after rollback:** `pnpm test` in `desktop-2/` returns to 389/389; `pytest` in `junior-backend/` returns to 391/391; `__APP_VERSION__` grep returns 6 hits in `src/**`.

### 11. Bug status transitions (post-merge)

| Bug ID | Before | After | Blocks CLOSED on |
|---|---|---|---|
| `BUG-006` | OPEN | FIXED_UNPROVEN | Doctor live walk on promoted bundle where `staged_bundle_path()` returns Some (pill == `current.json.version`). |
| `BUG-007` | OPEN | FIXED_UNPROVEN | Live customer walk verifying the 3 named surfaces (Settings copy, DiagnosticsSection stat, IntroSplash tag) all display the same string as TopHud pill on a promoted bundle. |
| `BUG-009` | OPEN | FIXED_UNPROVEN | 30-min tail of healed backend log showing zero `update_beacon_check_failed` events. |
| `BUG-012` | OPEN | OPEN (investigation logged + STOP report) | Native fix in `runtime.rs::runtime_check_now` (single line — `cache_active_root(&app)` after staging) then integration hot-swap test. |

> **Reminder:** No bug flips to CLOSED in an Impact Report.

### 12. Live customer walkthrough steps (for FIXED_UNPROVEN → CLOSED prep)

For BUG-006 · BUG-007:
1. Promote a runtime bundle with a distinctive version string (e.g. `9.9.9-wave-b1-proof`).
2. Cold-boot the installed shell. → Expect: TopHud pill + IntroSplash v-tag + DiagnosticsSection stat + Settings copy-diagnostics all read `v9.9.9-wave-b1-proof`.
3. Trigger a second bundle promotion mid-session while the app is open.
4. UpdateBeacon appears bottom-right. → Expect: TopHud pill flips to new version WITHOUT clicking Reload (hook subscribed to `lc:runtime-staged`).

For BUG-009:
1. Point desktop at a fresh Railway backend with no seeded manifest.
2. Wait 15 min. Tail `/tmp/backend.log` (or Railway logs) → Expect zero `update_beacon_check_failed` after the first minute.
3. Trigger a transient 500 (temporarily rename runtime_manifests table). → Expect one failure logged, then silence.
4. Restore the table. Wait 5 min for next poll. → Expect first healthy read clears the ring; a subsequent transient 500 logs again.

### 13. Doctor Lite verdict

Not run in-wave. Impact Report + tests + STOP report are the wave contract deliverables; Doctor Lite fires at Barrier 2 per the dispatch rules in `OWNERSHIP_MATRIX_TRAIN_B.md`.

### 14. Reviewer sign-off

- **Ship-lens:** deferred to Barrier 2 (per Train A precedent; Wave B1 did not touch any ship-lens-tracked visual surface beyond the pill/tag/stat contents, which are one-line renders reading the canonical hook).
- **Human reviewer:** integration lead (pending merge).
- **Rollback rehearsed:** no (single-commit revert is the plan).
- **Merged:** no.

### 15. Bug class + class-elimination progress (DECISION-0011)

| Bug ID | Bug class (BC-XXX) | Class-elimination pattern applied | Class instances eliminated by this commit | Class status after commit | Deferred class-elimination ticket |
|---|---|---|---|---|---|
| `BUG-006` | BC-002 · multi-source-of-truth | Single canonical hook + event-subscribed re-read | 1 (TopHud pill · already the P1-C target · now also event-subscribed) | in-progress (Rust `runtime_info` still fallback-shell in null-staged-bundle case · tracked in BUG-006 fixed-unproven notes) | none |
| `BUG-007` | BC-002 · multi-source-of-truth | Sweep every render site onto canonical hook · grep guard test | 5 (Settings · DiagnosticsSection · IntroSplash · App.tsx flowTrace · App.tsx logDiag · telemetry/bootstrap) | closed-app-wide for `state.runtime-version` (grep guard proves) | none |
| `BUG-009` | BC-005 · telemetry unbounded / observability drift | Backend degrades DB failure → 204 · client dedup ring | 2 (backend 500 path · client per-tick failure log) | closed-instance-2 (the two known observability drifts on `station.update-beacon.check`) | none |
| `BUG-012` | BC-005 (potentially) — but this instance is actually a Rust-cache-invalidation bug, not classic multi-writer observability | Not applied — native fix required | 0 | not applicable · single-instance native bug | native fix escalation (STOP report) |

### 16. Eight-question auto-answers per DECISION-0010

**BUG-006:**
1. Golden paths blocked (before → after): Ops "which version am I on?" partially blocked (correct only when Rust `runtime_info` returned real value) → substantially reliable (event-subscribed so mid-session flips too).
2. Business capabilities degraded (before → after): `capability.operational-excellence` LOW-degraded → healed for the runtime-active branch.
3. Canonical states affected: `state.runtime-version`.
4. Journeys that fail (before → after): `j014-runtime-update` AMBER → GREEN.
5. Telemetry that should have detected: pre-fix — none. Post-fix — `runtime_version_read_failed` diag when the hook falls back.
6. Tests that should have failed (before → after): pre-fix — none (no hook tests). Post-fix — `useRuntimeVersion.test.ts` proves fallback + event subscription.
7. Sibling bugs by root cause: BUG-007 (same divergent-reader class) · BUG-012 (upstream Rust cache stops the ideal fix landing).
8. Permanent architectural fix (referenced in §15): single canonical hook + event subscription. No new hook, no new state — just the existing P1-C hook hardened.

**BUG-007:**
1. Golden paths blocked (before → after): "which version am I on?" answered inconsistently across 4 surfaces → answered from one source.
2. Business capabilities degraded (before → after): `capability.operational-excellence` LOW → healed.
3. Canonical states affected: `state.runtime-version`.
4. Journeys that fail (before → after): `j014-runtime-update` AMBER → GREEN.
5. Telemetry that should have detected: none (no unit-test-style comparison of the 4 surfaces was ever wired).
6. Tests that should have failed (before → after): pre-fix — none. Post-fix — 6-case grep guard.
7. Sibling bugs by root cause: BUG-006 (same divergent-reader class).
8. Permanent architectural fix (referenced in §15): same hook, sweep every reader, grep guard prevents regression.

**BUG-009:**
1. Golden paths blocked (before → after): none directly · Ops observability degraded → healed.
2. Business capabilities degraded (before → after): `capability.operational-excellence` MEDIUM (log noise blocks diagnosis) → healed.
3. Canonical states affected: `state.update-beacon.check`.
4. Journeys that fail (before → after): `j014-runtime-update` observability AMBER → GREEN.
5. Telemetry that should have detected: `update_beacon_check_failed` DID detect but drowned itself in noise.
6. Tests that should have failed (before → after): pre-fix — none. Post-fix — `test_runtime_manifest_shapes.py` (4 shapes) + `UpdateBeacon.test.ts` (dedup ring).
7. Sibling bugs by root cause: BUG-005 (notification badge drift on empty inbox · similar observability-degradation pattern).
8. Permanent architectural fix (referenced in §15): backend degrades DB failure to semantic 204 · client dedup by (step, reason).

**BUG-012:**
1. Golden paths blocked (before → after): Ops "reload to swap bundle" blocked → still blocked (native fix required).
2. Business capabilities degraded (before → after): `capability.operational-excellence` MEDIUM → still MEDIUM.
3. Canonical states affected: `state.runtime-uri-cache` (undocumented before this investigation).
4. Journeys that fail (before → after): `j014-runtime-update` reload step still fails.
5. Telemetry that should have detected: no telemetry currently observes URI resolver root. Add `boot_bundle_version` event (BUG-001 dependency) post-native-fix.
6. Tests that should have failed (before → after): no native test exists. Post-fix would require a Rust integration test.
7. Sibling bugs by root cause: BUG-001 (needs boot event to make hot-swap observable · reciprocal dependency).
8. Permanent architectural fix (referenced in §15): one-line `cache_active_root(&app)` insertion in `runtime_check_now` · documented in STOP report.

### 17. Provenance record

| File | Origin (commit / session / author) | Reviewed? | Provenance verdict |
|---|---|---|---|
| `desktop-2/src/lib/useRuntimeVersion.ts` | Wave B1 rewrite of e4ff1060 (state-drift trifecta P1-C) | yes (this wave) | permanent · single canonical reader for `state.runtime-version` |
| `desktop-2/src/lib/useRuntimeVersion.test.ts` | Wave B1 new (2026-07-12) | yes | permanent · protects BUG-006 + BUG-007 fixes |
| `desktop-2/src/App.tsx` | pre-thread (edited scoped) | yes | isolated edits in boot metadata blocks + declare-const removal |
| `desktop-2/src/lib/telemetry/bootstrap.ts` | pre-thread (edited scoped) | yes | isolated edits at import + `release` field |
| `desktop-2/src/sections/diagnostics/DiagnosticsSection.tsx` | pre-thread (edited scoped) | yes | isolated · switched VERSION_PLACEHOLDER to hook read |
| `desktop-2/src/overlays/IntroSplash.tsx` | pre-thread (edited scoped) | yes | isolated · v-tag reads hook |
| `desktop-2/src/design-os/routes/Settings.tsx` | pre-thread (edited scoped) | yes | isolated · handleCopyDiagnostics reads hook |
| `desktop-2/src/components/UpdateBeacon.tsx` | pre-thread (Wave B1 edited scoped) | yes | permanent · dedup ring added; failure logging semantics tightened |
| `desktop-2/src/components/UpdateBeacon.test.ts` | Wave B1 new | yes | permanent · protects BUG-009 fix |
| `junior-backend/app/routes/runtime.py` | pre-thread (Wave B1 edited scoped) | yes | permanent · manifest endpoint degrades DB failure → 204 |
| `junior-backend/tests/test_runtime_manifest_shapes.py` | Wave B1 new | yes | permanent · protects the 4 x 204 shapes |
| `lcos/09_BUG_LEDGER.md` | pre-thread (rows edited scoped) | yes | isolated · 4 rows updated, no schema changes |
| `lcos/graph/bugs.json` | Doctor-regenerated | yes | rewritten via Python json.dump indent=2; content changes limited to 4 status blocks + totals |
| `lcos/reports/rc1-sprint/STOP_REPORT_WAVE_B1_BUG_012.md` | Wave B1 new | yes | permanent · escalation record for BUG-012 native fix |

Unknown provenance triggers: none.
