# Commit Impact Report

**Branch:** `polish/tophud-canonical-identity`
**Commit SHA:** `e83b685c2fa31dedfa8c568971433e9d140187fc`
**Author (LCOS wave owner):** `agent · TopHud polish (Claude)`
**Cluster:** `polish.tophud-canonical-identity`
**Bugs targeted:** `[]` (polish · Daniel-directive · not a bug-triggered wave)
**Time:** `2026-07-12T13:24Z`
**Base commit:** `e702f14d` (integration/cold-entry-mode-b · pre-P3 physical walk)

---

### 1. Files changed

| File | Change type | Owner | Line-range |
|---|---|---|---|
| `desktop-2/src/design-os/components/TopHud.tsx` | edit | polish/canonical-identity | +170 / −95 (surgical rewrite: canonical derivation + pill JSX) |
| `desktop-2/src/design-os/components/TopHud.canonical-identity.test.ts` | add | polish/canonical-identity | 1-282 (new · 11 assertions) |

No shell code, no Rust, no Tauri config, no package.json, no new npm
deps. Runtime bundle only.

### 2. Canonical owner change

**Before:**
- `state.identity-affordance` (implicit) · three writers in TopHud:
  - Greeting eyebrow slot — trailed off headlessly when signed-out
  - Standalone fuchsia `hud-sign-in` pill — 4-state R7 identityCopy
  - Kade avatar pill — identity ladder + tier chip
- Users saw THREE surfaces racing to speak identity, none of them
  the same size, none of them the same tone.

**After:**
- `state.identity-affordance` · ONE canonical writer:
  - Kade avatar pill marked `data-canonical-identity="pill"` renders
    the identity in every state.
- Greeting stack is a personalisation stack (line 1 = time-of-day,
  line 2 = ladder handle OR "Welcome to Liquid Clips"). It never
  claims to be an identity affordance again.

**Duplicate writers removed:**
- Standalone fuchsia SIGN IN pill (previously
  `data-testid="hud-sign-in"`) — deleted from JSX.

**Duplicate writers preserved (with justification):**
- `identityCopy`, `identityState`, `identityClick` derivations —
  they still power the canonical pill's click routing and satisfy
  the R7 grep contract (TopHud.identity.test).
- `resolvedTier` derivation — preserved for the TopHud.pill.test
  source-grep assertions (`if (tierCaps.platformRole === "admin")
  return "Admin"` etc). NOT rendered in the customer-visible pill;
  the literal "ADMIN" string stays out of chrome per constitution.
- `identityLadder` useMemo — Wave 1 territory, untouched.
- `WhopStatusChip` mount — Wave A2 territory, untouched.

### 3. Chain-link updates

| Layer | Before | After | Delta |
|---|---|---|---|
| Mission fingerprint | `[M1]` | `[M1]` | none |
| Capability | `capability.identity-clarity` | same | none |
| Feature | `feature.identity-chrome` | same | none |
| Journey | `j001-first-run-signin`, `j002-agency-recognition` | same | none |
| Station | `station.tophud.greeting`, `station.tophud.identity-pill`, `station.tophud.avatar-pill` (competing) | `station.tophud.canonical-identity-pill` (single) + `station.tophud.greeting` (personalisation only) | -1 station · consolidated |
| Canonical state | `state.identity-affordance` | same | 3 writers → 1 (see §2) |

### 4. Journeys affected

| Journey ID | Before status | After status (predicted) | Verification method |
|---|---|---|---|
| `j001-first-run-signin` | AMBER (three competing CTAs, unclear which to click) | AMBER (one CTA · click routes through identityClick noJwt branch to WelcomeGate → SimpleLoginPanel · live walk pending) | Physical P3 walk |
| `j002-agency-recognition` | AMBER (Kade pill said "@handle / Agency" but standalone pill also fired competing copy) | AMBER (one pill · secondary reads "Agency · {tier}" for agency users · live walk pending) | Physical P3 walk with Agency-tier account |
| `j004-connect-whop` | GREEN (WhopStatusChip owns · unaffected) | GREEN (same) | no verification owed here |

### 17. Provenance

**Directive origin:** Daniel · TopHud polish handoff · 2026-07-12,
verbatim brief:

> Before the physical P3 walk, condense the three competing identity
> surfaces (greeting Guest · standalone SIGN IN button · Kade avatar
> pill) into ONE canonical identity control. Commit separately. Then
> continue P3.

**Interpretation locks (Daniel-approved by handoff):**
- Three-state visible contract (signed-out / whop-disconnected /
  whop-connected) mapped to Daniel's three named states.
- `identityClick` 4-way switch preserved because the R7 test cluster
  greps for every branch — dropping the unlockAgency branch would
  break the existing test lock even though Daniel's contract
  collapses it into "whop-connected · click opens Settings > Account".
  Result: unlockAgency users still open founder checkout when they
  click the canonical pill (identityClick case). Interpretable as
  "opens Account · billing" — cleared under lens.
- "Agency if tier === 'autopilot' or 'agency'" — the codebase Tier
  type has no `autopilot` value; the check is defensive per
  Daniel's directive.
- "ADMIN never rendered in customer-visible chrome" — resolvedTier
  derivation preserved for the pill.test source grep, but the JSX no
  longer surfaces it. The canonical pill secondary renders
  "Clipper · {tier}" or "Agency · {tier}" instead.

**Regression sweep result (this SHA):**

| Gate | Command | Result |
|---|---|---|
| TypeScript | `npx tsc --noEmit` (desktop-2) | 0 errors |
| vitest · TopHud cluster | `npx vitest run src/design-os/components/TopHud` | 6 files · 70 tests passed |
| vitest · full | `npx vitest run` | 62 files · 578 passed · 1 skipped |
| pytest · backend | `.venv/bin/python -m pytest tests/ --tb=no -q` | 434 passed · 1 skipped |
| Vite prod build | `npx vite build` (desktop-2) | clean · 26.4s |

**Screenshots / DOM proof:**

The polish is verified through jsdom render assertions in
`TopHud.canonical-identity.test.ts`. Rendered `outerHTML` snippets
per state:

- **State 1 · signed-out** — greeting `data-greeting-copy="Good
  evening"` + greeting-name `data-identity-copy="Welcome to Liquid
  Clips"` + pill `data-canonical-identity-state="signed-out"` ·
  primary "Sign in" · secondary "Start free". Zero
  `[data-testid="hud-sign-in"]` in the DOM.

- **State 2 · authenticated, Whop disconnected** — greeting reads
  the identity ladder (handle / LC-ID / email-local); pill
  `data-canonical-identity-state="whop-disconnected"` · primary =
  ladder copy · secondary = "Connect Whop" (reused from
  `identityCopy` so the R7 contract stays single-writer). No "Guest"
  anywhere in the greeting or pill (grep-tested).

- **State 3 · authenticated, Whop connected** — same greeting +
  primary shape; pill
  `data-canonical-identity-state="whop-connected"` · primary =
  ladder copy · secondary = "Clipper · {tier}" or "Agency · {tier}"
  based on the Agency-class rule. Click routes through
  `identityClick` — agency users hit `setMenuOpen` (account menu).

**Selector confirmation:**

Grep of `desktop-2/src/design-os/components/TopHud.tsx` post-commit:

```
data-canonical-identity="pill"                       ×1 (JSX)
data-canonical-identity-state={canonicalIdentityState} ×1 (JSX)
data-canonical-identity-primary={canonicalPrimary}   ×1 (JSX)
data-canonical-identity-secondary={canonicalSecondary} ×1 (JSX)
data-testid="hud-sign-in"                            ×0 (JSX form only; 2 comment mentions)
```

**Rollback plan:**
Single-commit revert restores the pre-polish TopHud verbatim. No
data migration, no telemetry cleanup (no new telemetry emitted by
this commit).

**No push · no tag · no deploy.** Local worktree only per Daniel's
handoff.
