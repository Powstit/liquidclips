{
  "name": "liquid-clips-edge",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker · edge-first ingestion for Liquid Clips. L1 Cache API → L2 KV → L3 Railway origin. Cache reads, queue writes, proxy the JWT mint.",
  "scripts": {
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260701.0",
    "typescript": "^5.4.5",
    "vitest": "^1.6.0",
    "wrangler": "^3.60.0"
  }
}
