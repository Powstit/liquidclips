fastapi[standard]>=0.115
uvicorn[standard]>=0.32
sqlalchemy>=2.0
psycopg2-binary>=2.9
pydantic>=2.9
pydantic-settings>=2.6
pyjwt[crypto]>=2.9
cryptography>=43
python-dotenv>=1.0
svix>=1.40
httpx>=0.28
openai>=1.50
# Control Tower #1 · 2026-07-09 — hosted Anthropic clip-judge proxy at
# /proxy/anthropic/clip-bundle. Backend holds the key so new users can
# clip without pasting one. Dollar-based quota per tier feeds Clip
# Economics + gross-margin tracking in Admin HQ Control Tower.
# 2026-08-27 — pinned exact (was an open >=0.40 range). Live Railway
# deploy was stuck on a stale cached install predating `temperature`
# support in Messages.create(), throwing "unexpected keyword argument
# 'temperature'" on every hosted-Anthropic clip-judge call and taking
# clip generation down entirely. The loose >= constraint didn't force a
# reinstall on redeploy; an exact pin does. Verified 0.120.2 supports
# temperature via direct signature inspection before pinning.
anthropic==0.120.2
apscheduler>=3.10
resend>=2.4
posthog>=3.5
stripe>=10.0
sentry-sdk[fastapi]>=2.0
# HQ Agent 5 · Admin Recovery (break-glass flow). bcrypt hashes the recovery
# PIN + auth code; pyotp mints the fresh TOTP seed returned on /verify.
bcrypt>=4.2
pyotp>=2.9
