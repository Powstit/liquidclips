# Iron Gates Registry · Liquid Clips desktop-2

LOCKED 2026-07-20 · Single index of every regression fence. Each gate has 4-5 layers per `feedback_never_regress_4_layer_defense.md`. Run all fences via `scripts/iron-gates.sh {fast|pr|release}`.

## Fences

| Gate ID | Invariant | Owning files | Layer 2 script | Layer 3 vitest | Negative control | Tier |
|---|---|---|---|---|---|---|
| **IG-UNWRAP-CMD** | No bare `.unwrap()` inside `#[tauri::command]` bodies | `src-tauri/src/*.rs` | `scripts/lint-no-bare-unwrap-commands.sh` | `src/lib/tauriCommandsUnwrapAudit.test.ts` | Rust panic hook writes `~/LiquidClips/.last-crash.json` | fast |
| **IG-RUST-PANIC** | No bare panic risk in production Rust outside command bodies | `src-tauri/src/*.rs` · `scripts/rust-panic-baseline.txt` | `scripts/lint-rust-panic-production.sh` | `src/lib/rustPanicProductionAudit.test.ts` | `scripts/fixtures/rust-panic-negative/unsafe.rs` (must fail) · `scripts/fixtures/rust-panic-positive/safe.rs` (must pass) | fast |
| **IG-CAPS-AUDIT** | Frontend plugin import ↔ Rust registration ↔ capability grant parity | `src-tauri/src/lib.rs` · `src-tauri/Cargo.toml` · `src-tauri/capabilities/default.json` | `scripts/lint-tauri-capabilities-audit.sh` (advisory) | `src/lib/tauriCapabilitiesAudit.test.ts` (hard fail) | — | fast |
| **IG-ASYNC-CMD** | Every `#[tauri::command]` is `async fn` (or has `SYNC-OK:` sentinel) | `src-tauri/src/*.rs` | `scripts/lint-tauri-async-commands.sh` | `src/lib/tauriAsyncCommandsAudit.test.ts` | — | fast |
| **IG-IPC-CONTRACT** | Rust cmd fns ↔ generate_handler! ↔ manifest ↔ invoke() literals stay in sync | `scripts/ipc-manifest.json` · `src-tauri/src/*.rs` · `src/**/*.{ts,tsx}` | (bash TBD) | `src/lib/ipcContractAudit.test.ts` + `.negative.test.ts` | 8 negative regex controls in `.negative.test.ts` | fast (vitest) |
| **IG-SIDECAR-CATCH** | No silent `void sidecar.x()` or bare `void invoke()` in high-stakes calls | `src/lib/sidecarSafe.ts` · every consumer | `scripts/lint-no-silent-sidecar.sh` | `src/lib/sidecarCatchAudit.test.ts` | AppShell.tsx onReject runtime observer | fast |
| **IG-AUTH-KEYCHAIN** | Only `authStorage.ts` invokes `secret_{get,set,delete}_jwt` | `src/lib/authStorage.ts` · every consumer | `scripts/lint-auth-broker.sh` | `src/lib/authBrokerAudit.test.ts` | Kill switch `lc:disable-keychain.v1` (default ON) + SessionResetButton recovery affordance | fast |
| **IG-AUTH-KEYCHAIN L5** | No `codesign --force --deep` / `rsync … /Applications` / `security *-generic-password` in shipping scripts outside allowlist | `scripts/lint-forbidden-shortcuts.sh` (allowlist) | `scripts/lint-forbidden-shortcuts.sh` | — | Allowlist is the negative surface | fast |
| **IG-COMPOSER-VISUAL** | MockComposerBody ↔ MockComposer.css data-* attribute pairings + z-index war invariants | `src/design-os/routes/MockComposerBody.tsx` · `src/design-os/routes/MockComposer.css` · `src/design-os/routes/Composer.tsx` | (source-text audit — no bash) | `src/design-os/routes/MockComposer.visualContract.test.ts` | 13 assertion rows include StickyKade z-index, greeting binding, mode/speed/turbo/layout data-* pipeline, slot A/B/C, nav data-active | fast (vitest) |
| **IG-COMPOSER-MISS-DIAG** | `capability_route_miss` telemetry carries `query_text` + frame.hook doesn't claim aspect intents | `src/design-os/routes/Composer.tsx` · `src/design-os/routing/router.ts` | `scripts/lint-composer-miss-diag.sh` | `router.placeholderCommands.test.ts` | — | fast |
| **IG-SESSION-RESET (IG-014-B/C/D)** | Auth keychain purge helper contract + WelcomeGate dual listener | `src/lib/authStorage.ts` · `src/components/auth/*.tsx` | `scripts/lint-session-reset-guard.sh` | `src/lib/authStorage.session-reset.test.ts` · `useAuth.test.ts` · `useAuth.drift-detection.test.ts` | — | fast |
| **IG-OTP-A/B/C** | Observable OTP mailer + honest /start response + non-silent frontend advance | backend `email.py` · `src/components/auth/SimpleLoginPanel.tsx` | `scripts/lint-otp-observable.sh` | `SimpleLoginPanel.otp.test.ts` | — | fast |
| **IG-GOLDEN-JOURNEY** | Sidecar-claimed export path must disk-verify before UI shows success; release evidence must match SHA + version | `src/lib/verifyExportedFile.ts` · `src/lib/goldenJourneyEvidence.ts` · `src/design-os/routes/ExportRoute.tsx` | (release-tier · not fast) | `src/lib/verifyExportedFile.test.ts` (10 tests) · `src/lib/goldenJourneyEvidence.test.ts` (14 tests) | Fabricated evidence · non-existent path · synthetic preview stub · fs throws · stale evidence | release |
| **IG-RUNTIME-HOTSWAP** | UpdateBeacon must trigger `window.location.reload()` on `lc:runtime-staged` within the boot window so users pick up a new runtime bundle on FIRST relaunch (not the second) | `src/components/UpdateBeacon.tsx` | `scripts/lint-runtime-hotswap.sh` (6 required-element grep) | `src/components/UpdateBeacon.hotswap.test.ts` (8 tests) | Hostile edit of UpdateBeacon.tsx with reload/guard removed → fence fails on 6 counts | fast |
| **IG-BOOT-CHECK-THEN-SERVE** | KadeBootSplash MUST be the outermost mount + await `runtime_check_now` with bounded timeout + reload on version drift. Users never see the stale bundle (Discord / Slack pattern) | `src/components/KadeBootSplash.tsx` · `src/main.tsx` | `scripts/lint-kade-boot-splash.sh` (13 required-element grep across splash + main) | `src/components/KadeBootSplash.test.ts` (6 behavioural tests: browser fallthrough · loop-guard · reload-on-drift · dismiss-no-drift · offline · timeout) | Hostile splash without invoke/reload/loop-guard/timeout → fence fails; hostile main.tsx removing the wrap → fence fails | fast |
| **IG-UPDATER-COHERENT** (Updater v2 · 2026-07-20) | Rust updater is streaming + resumable + watchdogged + atomic-promoted + LKG-tracked + health-acked. Explicitly bans `.timeout()` on the reqwest Client builder (the pre-v2 bug). | `src-tauri/src/runtime.rs` | `scripts/lint-updater-coherent.sh` (24 required-element grep + 1 anti-match) | Rust unit tests in `runtime.rs` (Range/If-Range parse · stall watchdog · retry backoff · traversal guard · atomic promote) | Hostile edit removing streaming / resume / watchdog / backoff / promote / LKG / ack triggers the corresponding assertion | fast |
| **IG-BOOT-HEALTH-ACK** (Updater v2 · 2026-07-20) | Frontend invokes `runtime_ack_boot_healthy` after the app tree mounts so the Rust rollback trigger can observe unhealthy boots. | `src/lib/runtimeHealthAck.ts` · `src/App.tsx` | `scripts/lint-boot-health-ack.sh` (5 required elements) | `src/lib/runtimeHealthAck.test.ts` (delay + invoke + tauri-only) | Hostile edit removing the invoke path or the App.tsx call → fence fails | fast |
| **IG-COMPOSER-HOSTED-INTENT** (Composer wire · 2026-07-21) | SimpleComposer MUST call `requestKadeIntent` (hosted `/proxy/llm/intent`) BEFORE the local `routeIntent()` fallback · MUST pass `ALL_CAPABILITY_IDS` + session context · fallback preserved so the command bar never dead-ends offline / on 402/403 | `src/design-os/routes/SimpleComposer.tsx` · `src/lib/kadeIntentClient.ts` (client) · `junior-backend/app/routes/proxy_llm.py:308` (endpoint) | `scripts/lint-composer-hosted-intent.sh` (6 required-element grep · sentinel + import + call + capability_ids + fallback + context) | `src/design-os/routes/SimpleComposer.hosted.test.ts` (12 assertions: sentinel · import · catalogue · session context · fallback preserved · sidecar wires · engine event subscriptions · picker · progress+clip surfaces · discovery.scrub branch · logger) + `src/lib/classC.test.ts` (client contract) | Try/catch around `requestKadeIntent` with logged failure + local `routeIntent` fallback (Layer 4 runtime guard); hostile edit removing any grep target → fence fails | fast |
| **IG-COMPOSER-MODE-SWAP** (Composer swap · 2026-07-22) | The composer route hosts ONE `useComposerBrain` and swaps between SimpleComposerShell (idle) and MasterComposerShell (engaged) via `document.startViewTransition`. Both shells MUST read from `useComposerSession` (Zustand slot) · NEITHER shell owns handleSubmit or engine:* useEvent subscriptions · Kade avatar + command bar MUST carry matching `viewTransitionName` for the native morph | `src/design-os/routes/ComposerRoute.tsx` · `src/design-os/routes/useComposerBrain.ts` · `src/design-os/state/useComposerSession.ts` · `src/design-os/routes/SimpleComposerShell.tsx` · `src/design-os/routes/MasterComposerShell.tsx` | `scripts/lint-composer-mode-swap.sh` (12 guards: 5 file-exists · 3 sentinel · startViewTransition · isComposerEngaged · shell no-local-state · shell no-useEvent · Kade morph · command-bar morph) | `src/design-os/state/useComposerSession.test.ts` (11 assertions on `isComposerEngaged` predicate covering pristine · history · awaitingSource · progress · clips · activeSlug · lastReply · shellOverride idle/engaged/auto branches) | Runtime falls through to a plain `setState` when `startViewTransition` is unsupported so the swap still works in older browsers · dropping shellOverride='auto' defaults keep the auto-computed value | fast |
| **IG-SLO-DEFINED** (Reliability Sprint L5 · 2026-07-22) | Three canonical SLO targets frozen (error_rate < 1% · crash_free_session > 99.5% · p95_latency < 2000ms) · sloSink registered in bootstrap.ts · snapshot math computes breaches | `src/lib/telemetry/slo.ts` · `src/lib/telemetry/sinks/sloSink.ts` · `src/lib/telemetry/bootstrap.ts` | `scripts/lint-slo-defined.sh` (10 guards) | `src/lib/telemetry/slo.test.ts` (5 tests) | Frozen constants + sink registration; hostile edit removing sloSink registration or altering thresholds → fence fails | fast |
| **IG-FLAGS-DEFINED** (Reliability Sprint L6 · 2026-07-22) | Frozen feature-flag registry · every flag has enabled/rolloutPct/description/ownerContact · deterministic bucket hash · kill switch (enabled:false) overrides rollout · rollout-runner script for staged 10→50→100 | `src/lib/flags.ts` · `scripts/rollout-runner.sh` | `scripts/lint-flags-defined.sh` (10 guards) | `src/lib/flags.test.ts` (7 tests) | Object.freeze on FLAGS + short-circuit on !def.enabled; hostile edit unfreezing registry or removing kill-switch check → fence fails | fast |
| **IG-CHAOS-DEFINED** (Reliability Sprint L4 · 2026-07-22) | 5 fault-injection scripts + chaos-runner exist + EngineErrorBoundary mounted at App shell + SectionWithFallback on WalletDetail + sidecar client has retry/abort logic | `scripts/chaos/{chaos-runner,kill-sidecar,drop-network,inject-backend-500,race-conditions,disk-full}.sh` | `scripts/lint-chaos-defined.sh` (14 guards) | `src/lib/chaos.test.ts` (4 tests) | Each chaos script has a "Verify recovery" block — pass criteria enforce user-visible recovery paths | fast |
| **IG-RELIABILITY-SPRINT** (Layers 2 + 3 docs · 2026-07-22) | UAT protocol + SUS survey + task cards + recruitment email + analysis template all present · Heuristic evaluation doc covers all 10 Nielsen heuristics with severity bands | `docs/UAT_*.md` · `docs/HEURISTIC_EVAL_2026-07-22.md` | `scripts/lint-reliability-sprint-defined.sh` (18 guards) | (doc-scan · no vitest) | SUS scoring gate at 68 + 5-user Nielsen reference — hostile edit removing severity table or dropping a heuristic → fence fails | fast |
| **IG-KADE-BUBBLE-ACTIONABLE** (P0 H0-01 fix · 2026-07-22) | KadeSpeechBubble renders BOTH an action button (kind: diagnostics/retry/settings/signin/browse-supported) and a Dismiss button when action provided · action click produces observable state (hash nav / bus emit) so no error state is silent | `src/design-os/components/KadeSpeechBubble.tsx` · `src/design-os/components/KadeSpeechBubble.css` · `src/design-os/bridge/events.ts` · `src/design-os/components/AppShell.tsx` | `scripts/lint-kade-bubble-actionable.sh` (14 guards) | `src/design-os/components/KadeSpeechBubble.test.tsx` (9 tests) | AppShell forwards safe.action through so classifier-defined actions reach the bubble; hostile edit dropping the action-button branch → fence fails | fast |
| **IG-COCKPIT-COMING-SOON-GUARD** (P0 H0-02 fix · 2026-07-22) | Record source tile click handler early-returns when data-status="coming-soon" · runtime guard, not CSS-only (pointer-events was theatre) | `public/mockup/composer-suite.html` | `scripts/lint-cockpit-coming-soon-guard.sh` (5 guards) | (inline JS · no vitest) | Handler bubble()s a "Coming soon in v2.4" message so click still has visible feedback | fast |
| **IG-SERVER-HEALTH-DOT** (P0 H0-03 fix · 2026-07-22) | Persistent backend health dot polls /healthcheck every 60s · 4 states (grey/green/amber/red) · red click → #/diagnostics · 5s AbortController timeout · consecutive failures escalate amber → red at threshold 3 | `src/design-os/components/ServerHealthDot.tsx` · `src/design-os/components/TopHud.tsx` | `scripts/lint-server-health-dot.sh` (7 guards) | `src/design-os/components/ServerHealthDot.test.tsx` (10 tests) | AbortController + click-to-diagnostics gives user visible recovery path when Railway is down | fast |

## Tiers

| Tier | Wired to | Includes | Wall clock |
|---|---|---|---|
| **fast** | `.githooks/pre-commit` | All lint scripts + fast vitest audits | ~5 s |
| **pr** | manual (`iron-gates.sh pr`) | fast + full vitest + `tsc --noEmit` + brand-kit drift + shell contracts | ~30 s |
| **release** | manual (`iron-gates.sh release`) | pr + Playwright user-lens + `cargo check` | ~2-5 min |

## Grandfathered exceptions

Each exception is listed with an approved reason:

- **`scripts/rust-panic-baseline.txt`** — 2 grandfathered Rust panic sites: `lib.rs:482` (HTTP builder fallback) and `lib.rs:668` (main run.expect). Both unrecoverable init paths. Frozen shell prevents in-place sentinel; baseline is the escape hatch.
- **`scripts/lint-forbidden-shortcuts.sh` ALLOWLIST** — 5 files allowed to reference forbidden strings: the release signing script, the SessionResetButton user-affordance, this scanner itself, the session-reset lint guard (greps for the string as part of its own contract), and the session-reset vitest.

## Deferred work (requires greenlight)

- **Fence 2 · Cargo.toml deps** — `tauri-plugin-shell` and `tauri-plugin-clipboard-manager` are registered in `lib.rs:459-460` but not present in `Cargo.toml`. Next shell tag will fail `cargo build`. Blocked on `desktop-2/CLAUDE.md` shell freeze — needs explicit greenlight to add matching deps.
- **Fence 6 CI wire-up** — real ffmpeg export + evidence JSON generator + native macOS auth smoke on clean profile. Needs a CI runner with ffmpeg + a macOS runner slot. Currently release-tier is vitest + Playwright + cargo check only.
- **ExportRoute wire-up** — `verifyExportedFile` exists but ExportRoute.tsx still hasn't been rewired to THROW on `verified: false`. That is a real user-visible behavior change (previously silently showed success). Pending greenlight.

## Adding a new fence

1. Write the lint script under `scripts/lint-*.sh` with positive + negative fixtures.
2. Write a vitest twin under `src/lib/*Audit.test.ts` for the belt-and-braces layer.
3. Wire into `.githooks/pre-commit` AND `scripts/iron-gates.sh fast`.
4. Add a row to this table.
5. Add the LOCKED memory reference so agents know the fence exists.
