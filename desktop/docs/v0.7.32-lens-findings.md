# v0.7.32 — lens findings map

All 3 lens runs against the v0.7.32 diff, consolidated. Status as of the in-flight build (`biyc6fx2z`).

Diff under review:
- `src/components/PlatformBadge.tsx` (rewritten — brand SVG glyphs replace lucide-react)
- `src/components/schedule/ChannelRow.tsx` (NEW — ch-row pattern per mockup L150-160)
- `src/components/schedule/ChannelsManager.tsx` (modified — ChannelCard → ChannelRow swap)
- `src/components/schedule/ChannelCard.tsx` (DELETED — 327 LOC removed)

---

## Findings table

| # | Severity | Lens | Where | What | Status | Verified |
|---|---|---|---|---|---|---|
| 1 | **P0** | — | — | — | (no P0s from any lens) | n/a |
| 2 | P1 | ship-lens | `ChannelRow.tsx:41` | `case "deleted"` in `classifyStatus` is unreachable — `ChannelsManager.handleDelete` filters before re-render | **FIXED** — added comment explaining the case is here to keep the switch exhaustive against the enum | code only — will see in install |
| 3 | P1 | bug-hunt | `ChannelsManager.tsx:215,225,238,248` | `throw e` after `humanError(e)` in `handleTogglePause` / `handleDelete` — was load-bearing for ChannelCard's busy-state unwind, now no-op | **FIXED** — dropped the `throw e` lines, added comment explaining why | code only |
| 4 | P1 | bug-hunt | `ChannelsManager.tsx:206-228` | `_handleRename` + `_handleRefresh` parked as `void` — dead code with no callers | **FIXED** — both functions deleted; comment notes RPC plumbing is still in `backend.ts` if a future detail surface needs them | code only |
| 5 | P1 | bug-hunt | `PlatformBadge.tsx:14,183` | `import type React from "react"` + `export type { React }` — ghost re-export, no callers | **FIXED** — both lines deleted | code only |
| 6 | P2 | bug-hunt | `ChannelRow.tsx:85` | `@${channel.handle}` — if backend ever returns `@daniel` (TikTok occasionally does), result is `@@daniel` | **FIXED** — `channel.handle.replace(/^@+/, "")` guard added before re-prefix | code only |
| 7 | P2 | integration | `ClipCard.tsx:444` + `ClipWindow.tsx:187` | PlatformBadge `sm` is now **34×34** (was 28×28 — outline lucide-react). 6px bigger could overlap clip thumbnail corner UI | **PENDING VISUAL VERIFICATION** | requires install + snapshot |
| 8 | P2 | bug-hunt | `ChannelRow.tsx:87-101` | `handlePrimary`: if component unmounts mid-await, `setBusy(false)` fires on unmounted node | deferred | acceptable risk — toast pattern + cleanup flag is follow-up |
| 9 | P2 | bug-hunt | `ChannelRow.tsx:179` | Trash-icon click doesn't disable on `busy` — user can flip into confirmingDelete during in-flight delete | deferred | low-risk UX friction; same row already disables the toggle, mis-tap unlikely |
| 10 | P2 | bug-hunt | `ChannelsManager.tsx:381` | Connecting microstrip ml-7 indent — may be off by a few px from the row's visual rhythm | **PENDING VISUAL VERIFICATION** | requires install |
| 11 | P2 | bug-hunt | `ChannelsManager.tsx:200,204` | `onLinked` listener captures `finishConnecting` from closure; if useCallback deps change, stale ref | deferred | finishConnecting hasn't changed in v0.7.32; flagged for next refactor |
| 12 | P2 | integration | `AyrshareConnectionPanel.tsx` | Still uses chip-list + monochrome `PlatformIcon` outline glyphs; user sees Settings → Connections and Schedule → Channels with two different visual languages | **DEFERRED** — flagged for v0.7.33 polish sprint per the connections-drift-scope.html recommendation | not in scope for this turn |
| 13 | P2 | integration | Cockpit Routes modal | Mockup `cockpit-handoffs-demo.html:463-466` calls for ch-row pattern in the per-clip Routes modal; live app uses `ChannelPicker` chip pattern | **DEFERRED** — integration-lens verdict: "chip pattern is correct for multi-select picker"; revisit only if user testing flags it | not in scope |

---

## Specifically: "lines above the clip"

You meant the **outline / stroke icons on PlatformBadge as rendered above each clip thumbnail**. They appear in:
- `src/components/clips-feed/ClipCard.tsx:444` — bottom-left of every ResultsGrid card
- `src/components/workbench/ClipWindow.tsx:187` — top-right of the active workbench clip

**Code resolution: ✓ shipped.**
- `PlatformBadge.tsx` no longer imports lucide-react
- The 6 brand glyphs (Instagram camera silhouette, X wordmark, YouTube play box, TikTok music note, LinkedIn `in`, Facebook `f`) are inlined as `fill="currentColor"` solid paths from `clip-dashboard-demo.html:850-854` (the SOCIAL_SVG block you confirmed)
- No more outline strokes anywhere

**Install resolution: ⏳ pending.**
- The installed app is v0.7.31 — still shows the lucide-react outline icons
- The in-flight build (`biyc6fx2z`) will produce v0.7.32 — that build is the first one with the brand glyphs
- After sign-clean + local-install lands, the lines are gone

**Verification: requires snapshot-proof-lens after install.**
- Walk to Library tab → confirm a multi-platform clip's bottom-left badge shows solid colored brand glyphs, not outline icons
- Walk to Workbench → confirm the active-clip top-right badge matches
- Check whether `sm`=34 (was 28) busts the corner UI grid — finding #7 above

---

## What's left before "done"

1. In-flight build finishes (≈2 min remaining ETA)
2. sign-clean → /Applications install **(awaiting your confirmation per the new [[build-gate]] skill)**
3. snapshot-proof-lens walk on:
   - ClipCard badge (resolves finding #7 + the "lines above the clip" verification)
   - ClipWindow badge
   - Schedule → Channels (resolves connecting-microstrip alignment finding #10)
4. If finding #7 shows the 34×34 badge busts the corner grid → either expose a smaller variant or revert ClipCard to a `xs` size

No P0s from any lens, 4 P1s already coded into the diff, 4 P2s coded, 4 P2s deferred (low-risk + out-of-scope), 2 P2s require visual verification on the install.
