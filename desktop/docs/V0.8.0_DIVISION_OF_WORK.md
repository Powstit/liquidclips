# v0.8.0 — Division of Work: Kimi vs Claude

**Goal:** Ship v0.8.0 by end of day. Parallel execution, zero overlap, clear handoffs.

**Iron Gates (both of us):**
- Do NOT change color palette (fuchsia `#d946ef`, paper `#faf9f7`, ink `#1a1a1a`)
- Do NOT change font (Inter) or icon set (Lucide)
- Do NOT touch `liquidclips-marketing/*`
- Do NOT touch sidecar RPC registration pattern
- Do NOT remove legacy `SocialConnectionState` until v2 is 100%
- Do NOT store Ayrshare tokens on desktop

---

## KIMI — Backend, Data, Bugs, Performance

### P0 — Live Crashes (do first)
| # | Task | File | Time |
|---|------|------|------|
| 1 | Fix unhandled rejection: `sidecar.licenseJwtRead()` outside try/catch | `ScheduleQueue.tsx:184` | 10 min |
| 2 | Fix unhandled rejection: `backend.relinkChannel()` no catch | `AddChannelModal.tsx:456` | 10 min |
| 3 | Fix unmount race: `setState` without cancelled guard | `AddChannelModal.tsx:162` | 10 min |
| 4 | Fix unhandled rejection: `backend.listChannels()` no catch | `SchedulePage.tsx:71` | 10 min |
| 5 | Fix duplicate error: remove `setError(msg)` from cancel catch | `ScheduleQueue.tsx:174` | 5 min |
| 6 | Fix unmount race: fire-and-forget in `ChannelsManager` | `ChannelsManager.tsx:146` | 15 min |
| 7 | Fix single retrying ID → change to `Set<string>` | `ScheduleQueue.tsx:101` | 20 min |
| 8 | Fix `InlineScheduler` rendering disabled controls | `InlineScheduler.tsx` | 20 min |
| 9 | Fix empty state styling (bare `<p>`) | `ScheduleQueue.tsx:288` | 10 min |
| 10 | Migrate `socialGetConnection()` → `socialGetConnectionStrict()` | 4 files | 20 min |
| 11 | Defensive copy: `[...list].sort()` | `ScheduleQueue.tsx:131` | 5 min |
| 12-13 | Wrap "Open post" clicks in try/catch | `LocalQueue.tsx`, `ScheduleQueue.tsx` | 10 min |

**Subtotal: ~2.5 hours**

### P1 — Social Posting Data Layer
| Task | Detail | Time |
|------|--------|------|
| Create `usePlatformConnections` hook | Fetches `listChannels` + `socialGetConnectionStrict`, returns `isConnected(platform)`, `getChannel(platform)`, `refresh()` | 30 min |
| Create `ConnectionDot` component | 6px dot, green/amber/red/hidden + tooltip | 15 min |
| Wire smart click handler on `ClipCard` | Connect → reconnect → toggle route | 30 min |
| Create `InlineConnectPopover` | In-place OAuth mini-flow, 90s timeout, deep-link listener | 1 hour |
| Wire placeholder icons as clickable | Remove `pointer-events-none`, add inline connect trigger | 15 min |

**Subtotal: ~2.5 hours**

### P2 — Performance
| Task | Detail | Time |
|------|--------|------|
| Wrap `ClipCard` in `React.memo` | Biggest single win — rendered N times per project | 20 min |
| Wrap `BottomCockpit` in `React.memo` | Custom comparison on `focusedIdx`/`selectedIdxs` | 20 min |
| Add Vite `manualChunks` | Split `motion/react`, `lucide-react`, Tauri plugins | 20 min |
| Zustand selectors | `useAvatar` and `useWorkbenchStore` consumers | 20 min |

**Subtotal: ~1.5 hours**

### P3 — Thumbnail Studio Backend
| Task | Detail | Time |
|------|--------|------|
| Wire `method_thumbnail_preview_prompt` | Accepts config + item, returns prompt string | 15 min |
| Wire `method_thumbnail_save_brand` | Persist brand preset to `~/LiquidClips/brand_preset.json` | 15 min |
| Wire `method_thumbnail_save_identity` | Persist face crops to `~/LiquidClips/identity/` | 15 min |
| Wire batch generation loop | Groups of 11, background job, progress emit | 1 hour |
| Wire Solo tier paywall trigger | Reuse existing checkout flow, gate on `canAccessFeature('thumbnail_studio')` | 30 min |

**Subtotal: ~2 hours**

### P4 — Misc Backend
| Task | Detail | Time |
|------|--------|------|
| Remove orphaned `social_link.rs` Rust code | Not used by current UI | 15 min |
| Add `useDeferredValue` to `LibraryTab` search | Prevents layout thrash on 200 projects | 15 min |

**Subtotal: ~30 min**

**KIMI TOTAL: ~9 hours**

---

## CLAUDE — Visual, Components, Motion, Assets

### P0 — Real Platform Brand Glyphs
Create 7 SVG files in `src/assets/platforms/`:
- `youtube.svg`, `tiktok.svg`, `instagram.svg`, `x.svg`, `linkedin.svg`, `facebook.svg`, `threads.svg`
- Monochrome variants (currentColor), sized 16px/24px viewBox
- Source from official brand kits, no AI generation

**Subtotal: ~1.5 hours**

### P1 — Component Implementation (from `docs/demo.html`)

**`PlatformBadge` v2**
- Real brand glyphs (not Lucide)
- Connection state dot overlay (green/amber/red)
- Size variants: sm/md/lg
- Tooltip on hover: platform name + connection status

**`PlatformIcon` v2**
- Add LinkedIn, Facebook, Threads glyphs
- Same size variants

**`ClipCard` States**
- Baking state: mini progress bar inside card (per-card pending strip)
- Failed state: red border + retry CTA
- Free-tier watermark: subtle "Upgrade to Solo" overlay on locked features

**`ToastHost`**
- 4 variants: info/success/warn/error
- HUD bracket corners
- Auto-dismiss 4s + manual close
- Stacking: max 3 toasts, newest pushes oldest

**`ConfirmDialog`**
- Branded with HUD brackets
- Primary/danger/ghost button variants
- Keyboard: Enter = confirm, Escape = cancel

**`LoadingSkeleton`**
- 9-card grid shimmer
- Not generic — crafted with fuchsia pulse on specific elements
- Used in `WorkspaceRoom`, `LibraryRoom`, `ScheduleRoom`

**Subtotal: ~4 hours**

### P2 — Empty States (all rooms)
Crafted empty state for each room with:
- HUD corner brackets
- Contextual copy ("what are we making?", "no channels routed yet")
- CTA button routing to correct action
- Atmosphere background image (from `demo-assets/`)

Rooms:
- `WorkspaceRoom` — "Drop a video or paste a URL"
- `LibraryRoom` — "No archived clips yet"
- `EarnRoom` — "Browse bounties to earn"
- `ScheduleRoom` — "No posts scheduled" / "No channels connected"
- `SettingsRoom` — (if applicable)

**Subtotal: ~2 hours**

### P3 — Room Visual Refresh
Implement from `docs/demo.html` into actual React:

**`WorkspaceRoom`**
- Cursor-reactive fuchsia aurora background
- HUD bracket cards
- Per-deck atmosphere swap
- Drop-target overlay on drag

**`ScheduleRoom`** (currently ❌ in Figma tree)
- Queue lane with HUD bracket rows
- Channel loadout with real glyphs
- Analytics view with chart placeholder
- Schedule confirm celebration burst

**`LibraryRoom`**
- Search bar with `useDeferredValue` glow
- Filter tabs (All / Drafts / Published / Archived)
- Restore from archive action

**`SettingsRoom`**
- Account tab
- Channels tab (re-use channel rows)
- API keys tab (OpenAI key input with link to platform.openai.com)
- Billing tab (Solo tier status + upgrade CTA)

**Subtotal: ~4 hours**

### P4 — Motion & Polish
- **SpringRipple** on all primary buttons (already in demo)
- **GlowHalo** on cockpit cursor spotlight
- **WorkingBar** pulse on loading states
- **CommandPalette** hint in nav (not full implementation — just the hint text + visual)
- **KeyboardOverlay** — press `?` to see shortcuts (overlay only, no full shortcut system)

**Subtotal: ~2 hours**

### P5 — Cockpit Panel
- Collapsed state (44px) — already in demo
- Expanded state — already in demo
- Tab strip: Reaction / Caption / Trim / Style / Schedule
- Master CTA: Publish / Schedule
- **Thumbnail Studio tab** (new) — wire Kimi's backend methods

**Subtotal: ~2 hours**

**CLAUDE TOTAL: ~15.5 hours**

---

## Handoffs & Dependencies

### Kimi → Claude
1. **`usePlatformConnections` hook** → Claude wires into `PlatformBadge` + `ClipCard`
2. **`ConnectionDot` component** → Claude positions it on `PlatformBadge`
3. **Thumbnail Studio backend methods** → Claude builds the UI tab in cockpit
4. **Solo tier paywall trigger** → Claude styles the paywall card

### Claude → Kimi
1. **Real platform SVGs** → Kimi wires into `PlatformIcon`/`PlatformBadge` components
2. **`ToastHost` component** → Kimi wires into schedule bug fixes (error surfacing)
3. **`ConfirmDialog`** → Kimi wires into delete/retry actions
4. **`LoadingSkeleton`** → Kimi wires into `ScheduleQueue` + `ChannelsManager`

### No Dependencies (Parallel)
- Kimi's bug fixes (P0) — no design needed
- Kimi's performance (P2) — no design needed
- Claude's brand glyphs (P0) — pure asset creation
- Claude's motion polish (P4) — additive, won't break existing code

---

## Cut Line (if time runs out)

**Drop these first:**
1. CommandPalette (full implementation) — keep the hint only
2. SoundManager — all 5 audio cues
3. KeyboardOverlay — full shortcut system
4. 7 more unique clip thumbnails (keep reusing 1-5)
5. Confetti reel on first publish
6. Analytics charts (keep placeholder)
7. EarnRoom native discover (keep bounty embed)

**Keep these no matter what:**
1. All 13 schedule bug fixes
2. Social posting click-to-connect
3. Platform brand glyphs
4. Empty states for all rooms
5. ToastHost + ConfirmDialog
6. Thumbnail Studio tab (the money feature)
7. React.memo on ClipCard + BottomCockpit
