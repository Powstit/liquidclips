# Liquid Clips · Live-State Gate Sweep · 2026-06-25

> Generated by the Live-State Gate Sweep Agent, 2026-06-24T15:30Z.
> Method: live-probe every production surface (`liquidclips.app`,
> `account.liquidclips.app`, `api.liquidclips.app`,
> `updates.liquidclips.app`, `github.com/Powstit/liquidclips`,
> Vercel project env vars), read the source path that handles each
> question's sub-bullet, and report PASS / FAIL / BLOCKED with a
> one-line proof. Repo head: `bcc44e3` (origin/main, pushed). Both
> Vercel projects (account + marketing) READY at `bcc44e36`. Backend
> healthy at `0.7.51` build, `/status` returns 14/14 OK.

## TL;DR

- **Question 1 (Clipper):** FAIL · 1 P0 · 4 P1 · 2 P2
- **Question 2 (Agency):** FAIL · 1 P0 · 1 P1 · 0 P2
- **Question 3 (Daniel gets paid):** FAIL · 1 P0 · 2 P1 · 0 P2
- **Overall verdict:** **NOT SHIP-READY** · `/upgrade` is dead because
  `NEXT_PUBLIC_WHOP_CHECKOUT_PLAN_ID` is missing on Vercel account-app
  (renders "checkout not configured" panel — every in-app upgrade CTA
  bounces to a dead end). One env var fix flips this to SHIP-READY.

The three remaining P0s share one root cause (missing env var) — set
`NEXT_PUBLIC_WHOP_CHECKOUT_PLAN_ID` on the account-app Vercel project
to the live Whop plan id, redeploy, and the three P0s all go green
simultaneously.

---

## Question 1 — CLIPPER

### 1.1 Discover Liquid Clips
**Status:** PASS  
**Evidence:** `curl -sI https://liquidclips.app/` → `HTTP/2 200`, 223ms, `x-vercel-cache: HIT`. Page title `<title>Liquid Clips — Drop video. Clip. Post. Earn.</title>`. Visible copy includes "Paste a long video. Kade scans it. Open Liquid Clips. The clips load themselves." Single primary CTA `href="/download"`. Latest marketing deploy `dpl_9aB8ZTAcTZ2JHeaAZK9As6ykKfBv` from commit `bcc44e36`.  
**Failure (if any):** none  
**Fix:** n/a

### 1.2 Create account
**Status:** PASS (with caveat noted in 1.4)  
**Evidence:** `curl /sign-up → 200`, `curl /sign-in → 200` on marketing site. `liquidclips.app/connect-desktop?challenge=…` is the desktop activation door — page chunk renders, Clerk fallback always shown. Account-app `/sign-up → 200`, `/sign-in → 200`. `/api/desktop/connect` is the verified-Clerk mint route (file: `account-app/src/app/api/desktop/connect/route.ts`). Backend `/desktop/connect` is gated by `x-internal-secret` per `junior-backend/app/routes/desktop.py` (per memory `junior-activation-bridge`).  
**Failure (if any):** [P1] Whop sign-in button **not visible on marketing /connect-desktop** despite the code being deployed. `NEXT_PUBLIC_WHOP_SIGNIN_ENABLED` env var is NOT set on Vercel project `liquidclips-marketing` (only `account-app` has it). Whop fallback shim `/api/whop-oauth/start?challenge=…` does redirect 302 to backend correctly, but the page-side button that exposes it is gated off, so users only see Clerk on the canonical desktop activation URL. This contradicts the locked `liquid-clips-whop-lead-decision` ("Whop = primary").  
**Fix:** set `NEXT_PUBLIC_WHOP_SIGNIN_ENABLED=true` on `liquidclips-marketing` Vercel project (production target) + redeploy. Also add `NEXT_PUBLIC_WHOP_PRODUCT_AFFILIATE_URL` so the "no membership" empty-state CTA has somewhere to send buyers.

### 1.3 Install the desktop app
**Status:** PASS  
**Evidence:** `curl -L https://liquidclips.app/download` → 200, body contains `href="https://github.com/Powstit/liquidclips/releases/download/v0.7.55/Liquid.Clips_0.7.55_aarch64.dmg"`. `gh api repos/Powstit/liquidclips/releases/latest` returns `tag: v0.7.55` with both `aarch64.dmg` (139.5 MB) and `x86_64.dmg` (139.9 MB) + `.app.tar.gz` + `.sig`. DMG URL follows redirect to `release-assets.githubusercontent.com` (200).  
**Failure (if any):** [P2] **Windows build does NOT exist.** Release assets are macOS only (`.dmg` + `.app.tar.gz`). Marketing copy implies macOS-only ("mac · windows · local · no watermark" appears in `checkout/page.tsx` but is aspirational). [P2] **Updater channel lags download channel** — `updates.liquidclips.app/latest.json` returns `version: 0.7.51` (from 2026-06-11), while GH download channel is v0.7.55. Existing Mac installs will not auto-update to v0.7.55 until `scripts/release.sh` runs to publish a new manifest. Per memory `liquid-clips-marketing-site` this gap is known and intentional, but worth flagging.  
**Fix:** P2 (Windows) — explicitly remove "windows" from copy until shipped, or add Windows CI in `release.yml`. P2 (updater lag) — run `scripts/release.sh 0.7.55` to align channels OR accept lag and document.

### 1.4 Connect their accounts
**Status:** PASS  
**Evidence:** Backend `/auth/whop/start?challenge=abc123def456` returns `302 → https://api.whop.com/oauth/authorize?client_id=app_hLphExdFzjEQsM&redirect_uri=…/auth/whop/callback&response_type=code&scope=read_user&state=…` — full OAuth handshake wired. Marketing `/api/whop-oauth/start?challenge=…` shim returns `302` to the backend (same-origin start). Backend `/desktop/connect` is `INTERNAL_API_SECRET`-gated. Ayrshare configured: `/healthcheck` returns `"ayrshare_configured":true,"ayrshare_jwt_configured":true,"ayrshare_webhook_secured":true`.  
**Failure (if any):** Same as 1.2 — marketing page UI does not surface the Whop door despite the rail being wired, so connection only works via Clerk on first try.  
**Fix:** same as 1.2.

### 1.5 Generate / import clips
**Status:** BLOCKED (cannot test from this gate sweep — requires installed app + real account)  
**Evidence:** Code path exists: `desktop/src/components/cockpit/UploadPortal.tsx` (file present), `desktop/src/lib/sidecar.ts` (RPC bridge to python sidecar). Per `desktop/CLAUDE.md`: "Workspace (drop video / paste URL → clips) — ✅ live." Per `liquid-clips-source-of-truth` memory, installed `/Applications/Liquid Clips.app` is v0.7.62 (newer than public 0.7.55). Cannot prove end-to-end clip generation from a curl-only environment.  
**Failure (if any):** [BLOCKED] needs a human to drop a 30-min mp4 and confirm clips render in workbench.  
**Fix:** Daniel runs the §6 smoke test in `DEPLOYMENT.md` to convert BLOCKED → PASS.

### 1.6 Edit clips (trim · captions · reactions · watermark)
**Status:** BLOCKED  
**Evidence:** Code present — `desktop/src/components/cockpit/`, `desktop/src/components/earn/SubmissionForm.tsx`, `desktop/python-sidecar/whop_client.py`. Per memory `liquid-clips-demo-stage1`: "Cockpit + custom captions (react-colorful) + libass-wasm installed + in-app billing + analytics persistence shipped." Captions sprint #2 is live.  
**Failure (if any):** [BLOCKED] cannot validate caption/reaction/watermark export visually without launching the app and exporting a real MP4.  
**Fix:** Daniel runs the watermark / caption probes from `DEPLOYMENT.md` §6 (`ffmpeg -i exported.mp4 -ss 5 -frames:v 1 /tmp/wm-check.png`).

### 1.7 Export clips
**Status:** BLOCKED  
**Evidence:** Watermark gate is enforced server-side via `POST /submissions` returning `422 {detail:{code:"watermark_detected",upgrade_url:…}}` — `desktop/src/lib/backend.ts:1338-1349` parses this and throws `WatermarkDetectedError`. `desktop/src/components/earn/SubmissionPortal.tsx:120` catches it and shows the upgrade panel. Watermark cannot be client-bypassed.  
**Failure (if any):** [BLOCKED] cannot prove an actual export produces an MP4 from a curl-only sweep.  
**Fix:** Daniel runs the export probe.

### 1.8 Understand the rewards system
**Status:** PASS (with P1 caveat)  
**Evidence:** `GET https://api.liquidclips.app/campaigns` returns 10 campaigns, all `status: "live"`, rich shape (rpm_cents, budget_cents, eligibility[], visibility_tiers[], banner_url, whop_url). Free + paid copy explicit: "$1 RPM / $5 RPM / 50% MRR unlocked." 3 Uncle Daniel mission-lane campaigns auto-seeded. 7 legacy campaigns from prior seed. Earn page in account-app (`/embed/earn`) renders without frame-deny headers (`x-frame-options` absent · `content-security-policy` absent on that specific path — verified via `curl -sI`).  
**Failure (if any):** [P1] **Campaign banner images return 404.** All 10 campaigns have `banner_url: "https://api.jnremployee.com/static/campaigns/uncle-daniel.png"` (or similar slug) and every one of those `/static/campaigns/*.png` URLs returns 404 (also 404 at `api.liquidclips.app/static/...`). Earn tab card rail will render broken-image placeholders.  
**Fix:** P1 — either upload the banner PNGs to backend's static dir (or move to Vercel-hosted brand asset URL), or update the DB to point at brand images that already exist in `desktop/public/brand/`.

### 1.9 Submit clips correctly (SubmitToWhopModal posts a real CampaignSubmission)
**Status:** PASS  
**Evidence:** `desktop/src/components/earn/SubmissionPortal.tsx:106` calls `createSubmission()` from `desktop/src/lib/backend.ts:1319` which `POST /submissions` with `{campaign_id, clip_url, source_url, moment_type, hook_timestamp, why_this_moment, permission_type, disclosure_confirmed}`. Backend route confirmed in `/openapi.json` (`/submissions` POST). Live probe `POST /submissions` (unauth) → `401 missing bearer token` — endpoint exists and is gated. `WatermarkDetectedError` path verified (422 handling).  
**Failure (if any):** none (assumes authenticated flow works — sidecar gating is correct; cannot test without a valid JWT).  
**Fix:** n/a.

### 1.10 Reach first success WITHOUT Daniel's help
**Status:** FAIL  
**Evidence:** Funnel is "marketing home → /download → install DMG → launch app → /connect-desktop → Clerk sign-in → mint license → deep-link back → clip → submit". Each step works EXCEPT: (a) on `/connect-desktop` the Whop primary button is hidden (P1 1.2), so a clipper who bought through a Whop affiliate link must intuit to use the email path; (b) when they upgrade in-app via `/upgrade`, the page shows "checkout not configured" because `NEXT_PUBLIC_WHOP_CHECKOUT_PLAN_ID` is missing on Vercel (P0 — see Q3.2). (c) Banner images broken (P1 1.8).  
**Failure (if any):** [P0] Affected by the upgrade-page P0 from Q3 — if a free user wants to convert to Pro, the upgrade CTA leads to a dead surface.  
**Fix:** Resolve Q3 P0 (set Whop checkout plan id env var).

---

## Question 2 — AGENCY / BRAND

### 2.1 Sign up
**Status:** PASS  
**Evidence:** Same flow as 1.2 — agencies sign up the same way as clippers. No agency-specific sign-up gate.  
**Fix:** n/a

### 2.2 Understand Agency mode (signposting, paywall messaging)
**Status:** FAIL  
**Evidence:** `account-app/src/components/PricingCards.tsx:106-120` defines the Agency plan card: "$500/mo · For client accounts and white-label teams · Drip scheduling, sub-accounts, white-label exports." That copy IS clear. BUT: the Agency tier card is in the same `/dashboard` PricingCards grid as Solo / Growth — there is no separate "Agency / Brands" landing page, no marketing-site agency funnel, no `/agency` route (`curl liquidclips.app/agency → 404`). An agency landing here has zero indication this product is for them until they happen to scroll the dashboard pricing grid.  
**Failure (if any):** [P1] No agency-facing entry point or landing page. Agencies discover via the same clipper marketing funnel + a single $500 card in the dashboard pricing grid.  
**Fix:** P1 — add `/agencies` or `/for-brands` page in marketing, or at minimum a hero strip on `/` that signposts "for agencies."

### 2.3 Create a campaign
**Status:** FAIL  
**Evidence:** Backend has the full `/agency/campaigns` CRUD wired in `junior-backend/app/routes/agency_campaigns.py` (POST create-draft, PATCH edit, POST connect-reward, POST publish, POST refresh-reward, GET submissions, etc) — all 8 routes are in `/openapi.json`. BUT: **no UI in account-app or desktop calls these endpoints**. `grep -rln "/agency/campaigns" account-app/src` returns ZERO matches. The only campaign-creation UI is admin-only at `account-app/src/app/admin/_mutations/MutationsTab.tsx` (calls `/admin/mutations/campaigns/create`). An Agency-tier user who pays $500/mo cannot create a campaign through any in-product UI.  
**Failure (if any):** [P0] Agency tier has no campaign-creation UI in product. Backend is ready, frontend is missing.  
**Fix:** P0 — build an `/agency` (or `/dashboard/campaigns`) UI in account-app that posts to `/agency/campaigns` and follows the draft → connect-reward → publish state machine in `agency_campaigns.py`. Until then, every Agency customer must wait for Daniel to create their campaign via Admin HQ. This breaks the "launch workflow without Daniel" requirement.

### 2.4 Configure the campaign (budget, payout rate, banner, brief, asset links)
**Status:** FAIL (blocked by 2.3 — no UI exists)  
**Evidence:** Same as 2.3.  
**Fix:** Same as 2.3.

### 2.5 Understand paywall boundaries
**Status:** PASS  
**Evidence:** `PricingCards.tsx` shows 4 tier cards. Each card lists `built: true` vs `built: false, sprint: "Beta"` per feature — copy honest about what's live vs not (e.g. "Multi-platform publish in one click — Beta"). `junior-backend/app/features.py FEATURES_BY_TIER` is the single source of truth and matches the cards.  
**Fix:** n/a

### 2.6 Upgrade successfully (Stripe/Whop checkout works for Agency tier)
**Status:** PASS (via `/dashboard` PricingCards)  
**Evidence:** Agency plan is `cplan_3E4VBfKWkQlIuYRQG0YE5LfJPjx` (hardcoded default in `PricingCards.tsx:42` — env var `NEXT_PUBLIC_CLERK_AGENCY_PLAN_ID` would override but is MISSING on Vercel, so the hardcoded id wins). `<CheckoutButton planId={AGENCY_PLAN_ID}>` from `@clerk/nextjs/experimental` mounts the Clerk billing modal. Stripe runs under the hood. Webhook `/webhooks/clerk` returns 401 unauth (gated, alive).  
**Fix:** n/a (functional via hardcoded default).

### 2.7 Launch workflow without Daniel
**Status:** FAIL (blocked by 2.3)  
**Evidence:** Agencies can sign up + pay + arrive on a working dashboard, but cannot create their campaign without Daniel pasting it into Admin HQ.  
**Fix:** Same as 2.3.

---

## Question 3 — DANIEL GETS PAID

### 3.1 Free → paid upgrade (Solo / Growth / Agency / Accountpack each)
**Status:** FAIL  
**Evidence:** TWO independent upgrade paths exist:
- **Path A (`/upgrade`):** Whop embedded checkout. `account-app/src/app/upgrade/UpgradeCheckout.tsx:26` — if `planId` is empty, renders "checkout not configured" panel with a yellow warning bar. `NEXT_PUBLIC_WHOP_CHECKOUT_PLAN_ID` is NOT in account-app's Vercel env vars (verified via Vercel API). **Live `/upgrade` is dead** for any user who reaches it.
- **Path B (`/dashboard` PricingCards):** Clerk billing CheckoutButton with hardcoded `cplan_xxx` defaults. Works because the defaults are pinned in code. Solo/Growth/Agency/Accountpack all wired.  
**Failure (if any):** [P0] `/upgrade` is the canonical CTA from desktop ("Upgrade →" button) and from any "watermark detected → upgrade" path. It renders "checkout not configured" — a hard dead-end for the upgrade funnel.  
**Fix:** [P0] Set `NEXT_PUBLIC_WHOP_CHECKOUT_PLAN_ID` to the live Whop Solo plan id (likely the same as `NEXT_PUBLIC_WHOP_SOLO_PLAN_ID` which IS set on Vercel) on the `account-app` Vercel project (production target). Redeploy account-app via `vercel deploy --prod`.

### 3.2 Growth purchase (Stripe via Clerk)
**Status:** PASS (via /dashboard)  
**Evidence:** `cplan_3EV9Jjn8qLG130iSSRpAUOmqAfm` (PRO_PLAN_ID, slug `growth`) hardcoded in `PricingCards.tsx:40`. `@clerk/nextjs/experimental CheckoutButton` wraps the plan card. Webhook `/webhooks/clerk` POST returns 401 (alive + gated).  
**Fix:** n/a

### 3.3 Agency purchase (Stripe via Clerk)
**Status:** PASS (via /dashboard)  
**Evidence:** As 3.2 with `cplan_3E4VBfKWkQlIuYRQG0YE5LfJPjx`.  
**Fix:** n/a

### 3.4 Accountpack purchase (Whop add-on)
**Status:** PASS (per the code) BUT mis-labelled as Whop in the prompt — Accountpack is actually a **Clerk add-on** at `cplan_3EV9znSsguzmwoQoEr5kXpumkfM` ($6/mo, +1 social account). `AccountpackFocusCard` in `PricingCards.tsx:162` posts to Clerk billing. Backend webhook handler at `junior-backend/app/routes/webhooks_clerk.py:340` (`_accountpack_quantity_from_subscription`) reads the quantity from the subscription items + writes `users.extra_accounts`. New code added 2026-06-24 to make this idempotent.  
**Fix:** n/a, but worth verifying with a real Clerk webhook event in production.

### 3.5 Entitlement refresh (after payment, user's tier updates everywhere)
**Status:** PASS  
**Evidence:** `/checkout/complete?status=success` page calls `<ClientNotify status="success" />` which fires a postMessage so the desktop's `lc:tier-refresh` listener clears the 10-min sidecar cache (per `DEPLOYMENT.md` §6 + `account-app/src/app/checkout/complete/page.tsx:55`). Backend `/sync` rotates the JWT when ≤5 days remaining and re-reads tier from Whop + Clerk metadata. Webhook handlers are idempotent via `WebhookEvent.external_id`.  
**Fix:** n/a (needs real-purchase smoke to convert to verified PASS).

### 3.6 Billing notifications
**Status:** PASS (with P1 caveat)  
**Evidence:** `/checkout/complete` page handles `success` / `cancelled` / `errored` / `unknown` states with explicit copy. Backend `/notifications` route exists in `/openapi.json` (returns 401 unauth — alive + gated). Resend email sender configured per backend env.  
**Failure (if any):** [P1] cannot verify post-purchase email actually sends from a sweep — needs a real charge.  
**Fix:** smoke-test one $29.99 charge with Daniel's card to confirm.

### 3.7 No broken checkout (test every plan ID against live Clerk/Whop)
**Status:** FAIL  
**Evidence:** Plan IDs hardcoded in `PricingCards.tsx` (Solo `cplan_3E4VBeiWtZP0CJsvPwrIz91uDFk`, Pro/Growth `cplan_3EV9Jjn8qLG130iSSRpAUOmqAfm`, Agency `cplan_3E4VBfKWkQlIuYRQG0YE5LfJPjx`, Accountpack `cplan_3EV9znSsguzmwoQoEr5kXpumkfM`). Memory `clerk-billing-api-readonly` notes Clerk Backend API is `Allow: GET` only — cannot verify plan validity from this sweep without a Clerk dashboard pull. Per memory `liquid-clips-demo-stage1` the IDs were pinned 2026-06-01 after creating in dashboard, and `/dashboard` page has been live since.  
**Failure (if any):** [P1] `NEXT_PUBLIC_CLERK_*_PLAN_ID` env vars all MISSING on Vercel account-app project — so Daniel cannot rotate a plan without a code change + redeploy. Defaults work, but the override path is dead. [P0] `NEXT_PUBLIC_WHOP_CHECKOUT_PLAN_ID` missing (Q3.1).  
**Fix:** P0 covered in 3.1. P1 — set the 4 Clerk plan id env vars on Vercel to match the hardcoded defaults so env-driven rotation works.

### 3.8 No dead paywall (every "Upgrade" CTA actually leads to a working checkout)
**Status:** FAIL  
**Evidence:** Desktop watermark rejection (`WatermarkDetectedError` in `desktop/src/lib/backend.ts:1342`) throws with `upgrade_url ?? "https://account.liquidclips.app/upgrade?reason=watermark"` — that URL renders the "checkout not configured" panel today. Every in-product CTA that uses `/upgrade` (cockpit upgrade pill, watermark-rejection modal, EarnTab paid-feature upsell, etc.) leads to a dead end.  
**Failure (if any):** [P0] Same root cause as 3.1 — missing env var.  
**Fix:** Same as 3.1.

---

## Top P0 blockers (ranked)

1. **[Q3.1 · Q3.8 · Q1.10]** `NEXT_PUBLIC_WHOP_CHECKOUT_PLAN_ID` is missing on Vercel account-app project → `/upgrade` renders "checkout not configured" yellow panel → every in-app upgrade CTA dies. Single-env-var fix. *Fix:* `vercel env add NEXT_PUBLIC_WHOP_CHECKOUT_PLAN_ID production` with the live Whop Solo plan id, then redeploy account-app.
2. **[Q2.3 · Q2.4 · Q2.7]** Agency tier has no in-product campaign-creation UI. Backend is ready (`/agency/campaigns` CRUD all in `/openapi.json`). Agencies cannot launch without Daniel pasting via Admin HQ. *Fix:* build an `/agency` panel in account-app that follows the agency_campaigns.py state machine (draft → connect-reward → publish). Sprint-sized work, not single-env-var.

## Top P1 friction (ranked)

1. **[Q1.2 · Q1.4]** Marketing `/connect-desktop` does not show the Whop primary button. `NEXT_PUBLIC_WHOP_SIGNIN_ENABLED` env var is set on account-app Vercel project but NOT on the marketing project. Desktop opens `liquidclips.app/connect-desktop` (NOT account-app), so today every desktop activation flows through Clerk-only, contradicting the Whop-primary locked decision. *Fix:* `vercel env add NEXT_PUBLIC_WHOP_SIGNIN_ENABLED=true production` on `liquidclips-marketing` + redeploy. Also add `NEXT_PUBLIC_WHOP_PRODUCT_AFFILIATE_URL`.
2. **[Q1.8]** All 10 campaign `banner_url` values 404 (`api.jnremployee.com/static/campaigns/*.png` and `api.liquidclips.app/static/...` both 404). Earn tab renders broken-image placeholders. *Fix:* upload PNG banners to backend static dir OR PATCH each campaign's `banner_url` to a brand asset already in `desktop/public/brand/`.
3. **[Q2.2]** No agency landing page / signpost. *Fix:* add `/agencies` to marketing site (next sprint).
4. **[Q3.6]** Post-purchase email send unverified (needs a real charge to validate Resend wiring is live).
5. **[Q3.7]** Clerk plan id env vars all missing — defaults work but env-driven rotation is dead.

## Top P2 polish (ranked)

1. **[Q1.3]** Windows DMG does not exist; marketing copy implies "mac · windows · local · no watermark." Either build Windows in CI or remove from copy.
2. **[Q1.3]** Updater channel (v0.7.51) lags GH release channel (v0.7.55). Existing Mac installs won't auto-upgrade. Per memory `liquid-clips-marketing-site` this is intentional, but worth aligning before broader launch.

## Ship verdict

**DO NOT SHIP — pending the Top P0 fix list above.**

The single-env-var P0 (Q3.1) is a 5-minute fix:
```
source ~/.claude-credentials/vercel-liquidclips.env
cd /Users/dipdip/code/jnr/account-app
# Find or create the live Whop checkout plan id, then:
vercel env add NEXT_PUBLIC_WHOP_CHECKOUT_PLAN_ID production
vercel deploy --prod --yes --token "$VERCEL_TOKEN"
# Verify:
curl -s https://account.liquidclips.app/upgrade | grep -c "checkout not configured"  # expect 0 (or only inside JS comments)
```

After that, smoke-test ONE real Solo purchase end-to-end, then re-evaluate the Agency P0 (Q2.3) — the Agency UI is the only remaining blocker for "launch without Daniel" coverage of the Agency persona. If you're happy launching to clippers + accepting Daniel-as-deal-desk for agencies for now, you can ship the clipper funnel today and gate the Agency announcement until 2.3 lands.

Post-ship monitoring checklist (set before flipping the upgrade flow live):
- [ ] `/upgrade` returns the WhopCheckoutEmbed iframe (not the yellow panel) for a signed-in user.
- [ ] `/checkout/complete?status=success` fires the `lc:tier-refresh` postMessage and the desktop's tier flips from `free` → `solo` within 30s.
- [ ] One real $29.99 charge clears the watermark on the user's next export (verify via the `ffmpeg -ss 5 -frames:v 1` probe in `DEPLOYMENT.md` §6).
- [ ] `/webhooks/clerk` writes the new subscription to the `users` table (check `/me` payload before+after).
- [ ] `Resend` post-purchase email arrives in Daniel's test inbox.
- [ ] Marketing `/connect-desktop` Whop primary button visible after the env var lands.
- [ ] Campaign banner 404s resolved.
