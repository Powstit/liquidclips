# Liquid Clips · Bug List + Known Gaps · 2026-06-24

**Last updated:** 2026-06-24 night (before Daniel's sleep)
**Main HEAD:** `499c7f3` (HQ build + 9-fix remediation merged · LOCAL, not pushed)
**App % complete:** legacy `desktop/` ~82% · `desktop-2/` redesign ~55%

---

## ✅ RESOLVED THIS SESSION (2026-06-24)

| # | What it was | Where fixed |
|---|---|---|
| Payment-flow audit gaps | accountpack quantity bug · Solo/Whop return URL · legacy "pro" Clerk slug fallback | tasks #96-98 |
| Clipper publish broken | POST /publish-now from PublishModule | task #99 |
| Clipper analytics missing | Daily Ayrshare view-sync cron + /me/lifetime-views | tasks #100 |
| Clipper payout webhook missing | Whop submission_approved/rejected/paid handler with 3-fallback resolution | task #101 |
| Agency review endpoint missing | GET /agency/campaigns/{slug}/submissions | task #102 |
| Agency analytics missing | GET /agency/campaigns/{slug}/analytics | task #103 |
| SubmitToWhopModal emitted events only | Now POSTs /submissions with optimistic UI + rollback | task #104 |
| Real Whop carrot rail missing | app/whop_payments.py + /me/carrot/* endpoints (env-gated) | task #108 |
| HQ admin command center didn't exist | 5-agent parallel build · brand pass · 11 mutations · AI Terminal · recovery | tasks #109-114 |
| **Ship-lens BLOCK (3 P0 + 6 P1)** | Allowlist drift collapsed · audit-forge guard · AI health check · cost-cap removed · XFF Vercel signed · env-PIN deleted · Agents tab hidden until seeded · provider priority chain · refund endpoint DELETED entirely | task #115 |

---

## 🔴 OPEN BUGS / KNOWN GAPS (P0 — blocks shipping desktop-2)

| # | Issue | Where | Notes |
|---|---|---|---|
| BUG-001 | **Sidecar engine partly stubbed in `desktop-2/`** | `desktop-2/src/design-os/engine/sidecar-stub.ts` | Only 4 methods wired to real Python sidecar (ingest, startRun, getProject, exportClip). All other methods (regenerate_clip, get_captions, edit_captions, add_clip, thumbnails, agency campaigns, etc.) fall back to a 6-second mock progress emitter. Legacy `desktop/` is FULLY wired. ~2-week focused sprint to bring desktop-2 to parity. |
| BUG-002 | **Wallet page doesn't exist · clipper can't see pipeline money** | Spec at `docs/WALLET_PAGE_SCOPE.md` | Scoped tonight. Carrot withdraw button hidden = no silent-success lie, but no positive surface for "you've earned $X." Build is morning sprint (~1.5h agent run). |
| BUG-003 | **Settings page is SimPage stub** | `desktop-2/src/design-os/settings/` | Task #92 pending. |
| BUG-004 | **First-run onboarding is SimPage stub** | `desktop-2/src/design-os/onboarding/` | Task #93 pending. |
| BUG-005 | **Onboarding community rooms missing** | `junior-backend/scripts/seed_community_channels.py` | 9 production rooms exist. 4 onboarding rooms missing: start-here, install-help, first-clip-help, wins. Scoped — add 4 entries to seed script (~45 min). |
| BUG-006 | **CARROT_WHOP_LIVE never flipped → no real USDC has moved** | Railway env | Backend rail is built (task #108). Blocked on: onboard Whop sub-merchant master account · fund master USDC wallet · test with $1 carrot first. |

---

## 🟡 OPEN BUGS (P1 — degrades experience but not ship-blocking)

| # | Issue | Where | Notes |
|---|---|---|---|
| BUG-007 | **agent_personas table empty · Agents tab hidden until seeded** | `junior-backend/scripts/` (no seed for WHOP_AGENT_KEYS → agent_personas yet) | P1-007 remediation hides the HQ Agents tab when count==0. Seed script needed once Daniel decides the 2-3 agent personas. Per `liquid_clips_whop_lead_decision` memory: 2-3 personas, not 100 keys. |
| BUG-008 | **Whop main-lead OAuth on /connect-desktop incomplete** | Task #107 pending. Per `liquid_clips_whop_lead_decision`: Whop is PRIMARY, Clerk stays as fallback. Connect-desktop should offer Whop first. |
| BUG-009 | **AI Terminal Phase D (Codex + Kimi + action buttons)** | `account-app/src/app/admin/ai-terminal/` | MVP is Claude-only read-only. Phase D adds multi-provider + one-click action buttons (gated by typed confirmation). |
| BUG-010 | **Stripe/Whop sub-merchant onboarding UX for clippers** | When CARROT_WHOP_LIVE flipped, the existing backend endpoint exists but clipper-facing onboarding card needs polish. |

---

## 🟢 OPEN BUGS (P2 — cosmetic / scale)

| # | Issue | Notes |
|---|---|---|
| BUG-011 | IRON_GATES.md registry not updated with IG-HQ-001/002/003 | Pre-commit hook can't refuse deletion without registry entries. ~10 min to append. |
| BUG-012 | admin_audit_log payload_json LIKE-scan unindexed | Fine until ~10k rows. Migrate to JSONB + GIN index later. |
| BUG-013 | PIN setup form lacks double-entry confirmation | Standard pattern. Add a "Confirm PIN" input. |
| BUG-014 | Bug telemetry — function-heatmap shows admin's own visit as a hit | Cosmetic. |

---

## 🛡️ HARD CONSTRAINTS CARRYING INTO NEXT SESSION

- **Stay LOCAL** until Daniel reviews + greenlights push (446 commits ahead of origin, 13 from today)
- **No build, no install, no deploy** without Daniel saying so in the same turn
- **Backend env vars must be set** before /admin/* works:
  - `ADMIN_ALLOWED_IPS` (CSV; empty = fail-closed 404)
  - `JUNIOR_ADMIN_EMAILS` (CSV; 4 master emails, NO powstit)
  - `CLAUDE_ADMIN_API_KEY` (Anthropic console, server-side only)
  - PIN + auth code set via UI (`/admin/_security/PinSetup`), not env
- **IP gate uses signed `x-vercel-forwarded-for`** (P1-005), not the spoofable plain `x-forwarded-for`
- **Recovery is the break-glass** — exempt from IP gate, rate-limited 3/24h/IP, fast-path for allowlisted IPs (PIN only)
- **No push until Daniel reviews** the merged HQ + remediation work

---

## 📋 Morning sprint order (Daniel's stated priority)

1. **Build wallet page** per `docs/WALLET_PAGE_SCOPE.md` (~1.5h) → resolves BUG-002
2. **Seed onboarding rooms** (~45 min) → resolves BUG-005
3. **Ship decision** — Daniel reviews HQ + wallet + onboarding rooms, decides push timing
4. **(Tomorrow / next session)** Sidecar wire-up sprint for `desktop-2/` → resolves BUG-001 (~2 weeks)

---

## Resume command

```bash
cd /Users/dipdip/code/jnr && claude --resume d11d2b24-4ee6-48c4-8d62-09daf0dac363
# Then: "build wallet per docs/WALLET_PAGE_SCOPE.md, then seed onboarding rooms"
```
