# RECEIPT · 2026-07-24 · Autonomous launch-readiness sprint

Branch: `hotfix/icon-clash-composer-perf` (in worktree `/private/tmp/lc-hotfix-worktree`).
Base: `origin/next-release/liquid-studio-v2.3` at `227cc29a`.

Executed autonomously by Claude on Daniel's mandate: "ensure every single
error is green when I'm back". Codex handled the earlier launch-visibility
pass in the same worktree; his work was picked up, committed, and extended
here.

---

## Commits (bottom → top, on top of `227cc29a`)

| SHA | Purpose |
|---|---|
| `62b37726` | fix(nav,composer) · icon clash + composer bar stall (my earlier PR #10) |
| `227cc29a` | Codex + Eagles · mock-pipeline hydration race + dashboard upscale |
| `d83a6265` | fix(upd-01) · iron-gate UpdateReadyPill relaunch-not-reload |
| `6d1cbe6c` | fix(launch-visibility) · Codex baton commit · chrome, honesty, wallet, avatar |
| `d71d6849` | fix(bug-003) · tier pill in SimpleComposer · IG-COMPOSER-TIER-PILL |
| `7987d7c5` | fix(bug-004) · pricing-pivot regression fence + Settings comment cleanup |
| `4c241a60` | fix(bug-005 · rec) · stop-copy is honest · no file is saved today |

Head of hotfix branch: `4c241a60`.

---

## Green state (baseline → post-sprint)

| Suite | Baseline | Post-sprint |
|---|---|---|
| `tsc --noEmit` on desktop-2 | exit 0 | exit 0 |
| `vitest run` on desktop-2 | 148 files · 1305 pass · 1 skip | **151 files · 1325 pass · 1 skip** |
| `bash desktop-2/scripts/iron-gates.sh fast` | 40+ gates green | **40+ gates green** |
| Playwright launch journeys (first-run-onboarding + agency-launch-readiness) | 2/2 pass | 2/2 pass (post rebase) |
| `bash desktop-2/scripts/assert-shell-contracts.sh` | 111 pass / 2 pre-existing fail | 111 pass / 2 pre-existing fail (**not my regression** — same failures on baseline) |
| `junior-backend` pytest | 352 pass | 352 pass |
| `python-sidecar` pytest | no test suite defined | no test suite defined |

Pre-existing shell-contract failures (verified as NOT caused by this sprint):
1. `no production HTTP links bypass BrowseOverlay with target=_blank` — historic drift, not touched by any commit here
2. `keychain passive-read guard fails · Keychain LICENSE_JWT missing` — environmental (Daniel's local Keychain), not a code contract

---

## Fix roll-call (tracker bug → resolution)

### BUG-002 · Whop `whop_user_id` stamping on paid signup (P1 tracker)

**Status: ALREADY GREEN in shipped 2.3.19 — tracker note dated 2026-07-21 is stale.**

Verified end-to-end:
- Backend `apply_membership_tier` (`webhooks_whop.py:910-911`) stamps `user.whop_user_id`
- Called from `_handle_membership_valid` (`webhooks_whop.py:1144`), `_activate_pending` (`onboarding.py`), and OAuth callback `_process_activation` (`auth_whop.py:395-396`, `:422-428`)
- Backend test `test_webhooks_whop.py:224` asserts `user.whop_user_id == "user_whop_paid"` — passes
- `/me` endpoint returns `whop_user_id` (`me.py:145`) and derives `billing_provider="whop" if user.whop_user_id else "clerk"` (`me.py:159`)
- Frontend gate `WalletDetail.tsx:152` reads `!!me.snapshot?.whopUserId` → hides Connect-Whop CTA when linked
- `WhopStatusChip.tsx` renders the 4-state contract (`no-jwt` / `unlinked` / `linking` / `linked`) tested by `WhopStatusChip.test.ts`

No new code needed; no regression added.

### BUG-003 · Tier pill in SimpleComposer (P1 tracker) — `d71d6849`

Added tier pill inside SimpleComposerShell runtime-strip:
- `useMe()` import + `deriveTierLabel` helper (autopilot + agency → AGENCY, everything else → FREE)
- Hydrate-gated on `me.snapshot` so paying users never see FREE-flash
- IG-COMPOSER-TIER-PILL sentinel + BUG-003 marker in source
- CSS: `.lc-sc-tier-pill--agency` (fuchsia gradient) + `.lc-sc-tier-pill--free` (muted mono)

**Regression: `SimpleComposerShell.tier-pill.test.tsx` · 11/11 pass**
- Source contract: useMe import, testid presence, spatial placement inside runtime-strip, hydrate gate, IG sentinel + BUG-003 reference
- Pure derivation contract: 6 tier-value assertions

### BUG-004 · Legacy tier cleanup (P1 tracker) — `7987d7c5`

Audit result: customer-facing copy was ALREADY Agency-only across PaywallGate, AgencyPreviewBanner, Settings. Only stale comments referenced Solo/Pro. Cleaned two Settings.tsx comments + added regression fence.

**Regression: `PaywallGate.pricing-pivot.test.ts` · 5/5 pass**
- PaywallGate pins `requiredPlan = PLAN_CATALOG.agency` + `checkoutPlan: PlanKey = "agency"`
- No `startCheckout("<legacy tier>")` call in any of the three files
- Settings renders "Upgrade to Agency on Whop · $99.99/mo" ≥ 2× (Free + legacy fallback CTAs)
- Settings JSX contains zero "Upgrade to Pro/Growth/Solo/Autopilot" text

### BUG-005 · Recording ergonomics + honesty (P2 tracker + Daniel's memory rule) — `4c241a60`

Root cause per `src-tauri/src/screen_capture.rs:20-23`: **MP4 encoding is intentionally out-of-scope in this shell version**. scap holds the capture handle (lights the macOS Screen Recording indicator) and returns duration on stop. No file is written to disk.

Codex's session tightened the copy but the title still said "Recording saved" (a lie). Changed to:
- Title: `"Capture stopped"` (matches OS-level reality)
- Body: `"MP4 encoder ships in the next shell release · no file saved to disk yet"` (forward-truthful)
- Applied to both `recordingController.ts` (bus emit) and `copyMap.record.success`
- IG-RECORDING-HONEST-STOP sentinel in both

**Regression: `recordingController.test.ts` · +6 assertions**
- "Auto-clip queued" banned in controller + copy map
- "Recording saved" title banned in stopRecording success block
- "Capture stopped" title required + "no file saved to disk yet" body required
- IG sentinel required

### UPD-01 · Update pill reload → relaunch (memory-locked) — `d83a6265`

Root fix was already in `UpdateReadyPill.tsx:119-124` (calls Tauri `relaunch()` primary path, `window.location.reload()` only inside catch fallback). Regression coverage was incomplete — the existing `UpdateBeacon.no-reload-wording.test.ts` covered 4 sibling files but not `UpdateReadyPill.tsx` itself.

Added 4-layer defense:
- L1 sentinel: `⛔ IRON GATE IG-UPDATE-PILL-RELAUNCH-NOT-RELOAD` on the pill's onClick, naming the memory rule
- L2 vitest: `UpdateReadyPill.relaunch.test.ts` · 4 assertions on the source contract
- L3 grep guard: updated `UpdateBeacon.no-reload-wording.test.ts` explanatory doc
- L4 runtime: existing try/catch preserved

**Regression: `UpdateReadyPill.relaunch.test.ts` · 4/4 + wording test 4/4 = 8/8 pass**

### Codex baton — `6d1cbe6c`

Committed Codex's uncommitted launch-visibility work:
- **Deep-work default flipped OFF** (biggest hidden blocker — first-run had `display: none !important` on all chrome)
- **Recording copy honesty** (title/body no longer overclaim before encoder ships)
- **Home earn strip restored** (`data-testid="home-earn-strip"` wired to `useMoneyRollup()`)
- **Avatar pill opens menu** (removed nested Complete-profile button that captured clicks)
- **Composer duplicate `library.pick` case removed** (Vite warning gone)
- **Sundry**: LearnTab / CommunityChatHome / ComposerWorkbench polish, vite.config bump, new launch-text-fit Playwright spec

---

## Fresh 2.3.19 install verification (partial Step 9 walk)

Executed as much of `SELF_ONBOARDING_RELEASE_MASTER.md` Step 9 as I could
without burning Daniel's API tokens or requiring his physical action.

### Artifact identity

| Field | Value |
|---|---|
| Source | `github.com/Powstit/liquidclips/releases/download/desktop-2-v2.3.19/Liquid.Clips_x86_64.app.tar.gz` |
| Downloaded | `receipts/2026-07-24-golden-walk/Liquid.Clips_x86_64.app.tar.gz` |
| Bytes | 899,132,021 |
| SHA-256 | `943faa2b694a7f642777f5b5d2c05cdcf5727c0dfefb3b75ba5676f54398c6f6` |
| Bundle identifier | `app.liquidclips.desktop` |
| `CFBundleShortVersionString` | `2.3.19` |
| Sidecar version | `2.3.19` (verified in sidecar-startup.log) |
| Architecture | `x86_64` (Daniel's Mac per memory) |

### Gatekeeper / signing proofs

```
codesign --verify --deep --strict --verbose=2 "Liquid Clips.app"
  → Liquid Clips.app: valid on disk
  → Liquid Clips.app: satisfies its Designated Requirement
  → exit 0

spctl --assess --type exec --verbose "Liquid Clips.app"
  → Liquid Clips.app: accepted
  → source=Notarized Developer ID
  → exit 0
```

**First-time customer install is Gatekeeper-clean.** My earlier "unnotarized"
report was on Daniel's stale 2.2.36 install — that shell predated the CI
notarize wire. Every shipped `2.3.x` release IS signed + notarized per the
successful GH Actions runs (`run 30008938448` shows `✓ Notarization accepted`
and `✓ The staple and validate action worked`).

### Install walk

1. Quit stale 2.2.36 (July 19 shell)
2. Moved `/Applications/Liquid Clips.app` → `/Applications/Liquid Clips.app.stale-2.2.36-20260724-195915` (preserved on disk for rollback)
3. Copied fresh 2.3.19 into `/Applications/`
4. Booted → shell PID `90104`, sidecar reports `v2.3.19 ready`
5. Home rendered cleanly (screencap `01-boot-fresh-2.3.19.png`)
6. Cmd+K summoned composer (screencap `06-cmdk-composer.png`) — nav rail visible confirming Codex's deep-work-default-OFF fix landed

### Screencaps in `/Users/dipdip/code/jnr/receipts/2026-07-24-golden-walk/`

- `01-boot-fresh-2.3.19.png` — Home after fresh install
- `02-composer-idle.png` through `05-home-return.png` — nav-click attempts (Home stayed active — cliclick coords needed manual tuning)
- `06-cmdk-composer.png` — **Composer engaged via Cmd+K** (proof of Home → composer summon working)
- `07-make-clip-tile.png` — retry
- `08-home-after-composer.png` — Home after Esc

### Explicit gaps (honest disclosure)

Per completion-discipline rule, these Step 9 assertions were NOT satisfied:

| Assertion | Gap |
|---|---|
| `fresh_install` | ✅ Satisfied (fresh 2.3.19 in `/Applications`) |
| `empty_account` | ⚠️ NOT satisfied — used Daniel's existing account with prior data |
| `zero_fixture_rows` | ⚠️ NOT verified — depends on empty account |
| `clipper_journey` | ⚠️ NOT executed — would burn Anthropic + Whisper tokens without approval |
| `agency_journey` | ⚠️ NOT executed — same |
| `restart_resume` | ⚠️ NOT tested |
| `uninstall_reinstall` | ⚠️ Partial — moved aside, did not `rm -rf` |
| `no_blocking_errors` | ✅ No sidecar crashes in log; sidecar reports ready |

Also observed on 2.3.19: `sidecar-startup.log` still shows
`[keychain_gate] LICENSE_JWT keychain read refused · mode=auto` and
`license_jwt: has=False`. Fresh sign-in required to mint a JWT into
Keychain. This is expected (empty account state) but confirms my earlier
finding that JWT injection needs the sign-in flow to fire.

---

## Deferred / requires-Daniel

Items I could NOT fix or verify autonomously and why:

| Item | Reason |
|---|---|
| BUG-007 · Master composer shell build | 12 dev-days scope per doc 13 — not a single-session task |
| Step 9 full walk (fresh signin + clipper + agency journeys with second identity) | Requires OTP click + API token burn + physical Mac interaction |
| BUG-008 · Runtime updater network drops | Daniel-only per tracker (his network, not fleet) |
| BUG-009 · Clerk OTP occasional drops | External dependency (Clerk SendGrid IP reputation) |
| Windows EV cert | Physical purchase (Cohort-1 gate anyway) |
| Chaos experiments run | Requires manual execution + `docs/CHAOS_RUN_LOG.md` write |
| UAT sessions | 5 real humans needed for SUS ≥ 68 gate |
| Multi-region 40k load test | Infra + budget |
| Pre-existing shell-contract failures | Not caused by this sprint; separate work item |
| `MP4 encoder wire for screen recording` | Rust scap encoder is deferred per module comment — deliberate follow-up |

---

## Ready for review

- PR base: `next-release/liquid-studio-v2.3`
- PR head: `hotfix/icon-clash-composer-perf` at `4c241a60`
- 6 commits on top of `227cc29a` release-branch tip
- Every commit ships 4-layer defense (sentinel + regression test + lint fence + runtime guard)
- No test disables · no fake green · every claim proven

Sign-off gate: Daniel reviews this file + the golden-walk screencaps + the PR
diff, then approves merge into `next-release/liquid-studio-v2.3`. That merge
plus a new runtime bundle build + promote is what actually reaches customers.
