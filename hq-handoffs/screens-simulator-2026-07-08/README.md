# Screens Simulator + DMG Background · HQ handoff · 2026-07-08

Two shipping items in this bundle, both requested by Daniel while
prepping today's payment-tests / tomorrow's launch:

1. `spec.md` — build spec for the embeddable "every-screen simulator"
   iframe. HQ hosts once, marketing embeds anywhere.
2. `assets/dmg-background-distribution-wins.png` — 1536×1024 branded DMG
   installer background (Kade + Applications folder + "DISTRIBUTION
   WINS" tagline). Ready to crop into DMG chrome (see spec §6).

Both are marketing-adjacent — Claude wrote the spec + generated the
asset per the HQ handoff pattern (spec-first, HQ ships). No live
deployment yet.
