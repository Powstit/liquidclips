# Regression grep sweep · Phase 8 nav mounting 2026-07-04

Every Phase 7 hardening sweep must remain green. All 6 counts = 0.

## dangerouslySetInnerHTML in routes/**: 0
Command: grep -rn "dangerouslySetInnerHTML" /Users/dipdip/code/jnr/desktop-2/src/routes
Output: (empty · Phase 4 sweep preserved)

Phase 4 replaced 10 hits across 5 route files with `renderInline(str)`
from `desktop-2/src/components/safe-inline.tsx`. Phase 8 did not
touch any route file that renders port copy.

## `if secret:` fail-open in junior-backend routes: 0
Command: grep -rn "if secret:" /Users/dipdip/code/jnr/junior-backend/app/routes | \
         grep -v "if secret and" | grep -v webhook
Output: (empty · Phase 1 sweep preserved)

Phase 1 killed 14 inline fail-open sites and consolidated them
through `deps.require_internal_secret`. Phase 8 did not touch
junior-backend.

## lc-default-salt live default: 0
Command: grep -rn "lc-default-salt" \
         /Users/dipdip/code/jnr/liquidclips-marketing/src \
         /Users/dipdip/code/jnr/account-app/src \
         /Users/dipdip/code/jnr/desktop-2/src | \
         grep -v "Previous default"
Output: (empty · memorial comment only in referrals/click/route.ts)

Phase 2 hardening made the salt env-var required in production;
the only remaining occurrence memorialises the removal.

## real creator emails in routes: 0
Command: grep -rn "mkbhd\\.com\\|caseyneistat\\.com\\|emmachamberlain\\.com\\|
                    aliabdaal\\.com\\|simonegiertz\\.com\\|colinandsamir\\.com\\|
                    airrack\\.tv\\|cleoabram\\.com" \
         /Users/dipdip/code/jnr/desktop-2/src/routes
Output: (empty · sweep preserved)

Phase 7 audit confirmed the 8 real-creator domains do not appear
in the ported route trees. Phase 8's `AccountSection.test.ts`
adds a fresh assertion re-checking the same 8 domains against
`WalletDetail.tsx` to guard against future roster drift.

## CSS local grad literals in routes: 0
Command: grep -rn "\\-\\-.*grad-fuchsia\\|\\-\\-.*grad-paper" \
         /Users/dipdip/code/jnr/desktop-2/src/routes | grep "linear-gradient"
Output: (empty · Phase 6 consolidation preserved)

Phase 6 rewrote 8 port CSS files to reference `--grad-fuchsia` and
`--grad-paper` from `brandTheme.css`. Phase 8 did not touch any
route CSS.

## IRON GATE IG-NNN sentinels: 25 (baseline unchanged)
Command: grep -r "IRON GATE IG-" /Users/dipdip/code/jnr/desktop-2/src
Count: 25

Phase 8 did not modify any file adjacent to an IRON GATE sentinel.
Sentinel count matches the Phase 7 close baseline.

## Iron gate scripts

### bash desktop-2/scripts/lint-kade-decoupling.sh
Output: (0 lines) · exit code implicit success

### bash desktop-2/scripts/assert-kade-anchor.sh
Output: (0 lines) · exit code implicit success

### bash desktop/scripts/brand-kit-drift-check.sh
Status: exit 1 (PRE-EXISTING drift in legacy `desktop/` v0.7.x tree)
Not caused by Phase 8:
  git diff security/2026-07-04-hardening-pass..\
           feature/nav-mounting-2026-07-04 -- desktop/ = 0 lines

The IG-012 drift lives in `desktop/src/index.css` vs its own
`docs/demo*.html` mirrors. Phase 8 landed exclusively in
`desktop-2/` (v2.x) which does not run this legacy script.
Documented for transparency; not a Phase 8 regression.
