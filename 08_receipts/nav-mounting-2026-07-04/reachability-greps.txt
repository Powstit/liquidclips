# Reachability grep sweep · Phase 8 nav mounting 2026-07-04

Each mount must be reachable via at least one live reference in the
target file. All 6 counts satisfy the ≥1 requirement.

## Mount #1 · LoginActivation as unauth boot gate

  grep -c "LoginActivation" desktop-2/src/App.tsx = 5

Wired at AuthGate — replaces the LoginOnboardingRoute unauth render.
LoginOnboardingRoute preserved as a rollback breadcrumb via a `void`
reference so tsc's `noUnusedLocals` doesn't strip it.

## Mount #2 · SyncMailMoneyDrop → #/outreach section

  grep -rc "SyncMailMoneyDrop" desktop-2/src/sections/outreach = 9

Wired in the new `OutreachSection.tsx` wrapper. Registry entry lives
in `shell/sectionRegistry.ts` with `navVisible:false` until the Home
hero CTA copy is greenlit — router reachable via `#/outreach` hash
today.

## Mount #3 · WalletDetail inside AccountSection

  grep -rc "WalletDetail" desktop-2/src/sections/account = 19

`AccountSection.tsx` body is now `<WalletDetail />` alone. Prior stub
identity/tier/affiliate HUD cards (backed by `fakeAccount.preview`
fixture) removed — no user data lived there.

## Mount #4 · CatalogCarousel inside CampaignsSection

  grep -rc "CatalogCarousel" desktop-2/src/sections/campaigns = 18

Legacy `All campaigns` `lc-grid-3` block swapped for
`<CatalogCarousel />`. Section header · active-campaign detail card ·
Create + Watermark modal scaffolding preserved.

## Mount #5 · CancellationIntercept modal from Cancel button

  grep -rc "CancellationIntercept" desktop-2/src/sections/account = 8

Wrapped Mount #3 `<WalletDetail />` with corner-pinned Cancel
Subscription trigger + conditional modal. onKeep dismisses. onQuiet
also dismisses today with a TODO(phase-9) marker where the real
Whop cancel-subscription RPC plugs in (guard rail 12 · no new RPC
wrappers in Phase 8).

## Mount #6 · EmbedPreviewCard inside campaign builder

  grep -rc "EmbedPreviewCard" desktop-2/src/sections/campaigns = 14

Nested `<EmbedPreviewCard />` inside a `.campaign-preview-slot`
wrapper below the Mount #4 CatalogCarousel. Additive to Mount #4 · not
displacing.

## Section registry integrity

Every `getSectionByRoute("<route>")` call resolves post-Phase-8:
  home · browse · editor · projects · campaign · account · diagnostics ·
  hq · learn · outreach

The BUG-047 harness guard remains satisfied — `SECTION_OUTREACH` is
NOT listed in `DEPRECATED_SECTION_IDS`.
